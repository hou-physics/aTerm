# aTerm V3.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修好用户报的四件坏事——新对话身份丢失（连带标签标题/侧栏去重/对话面板/底栏模型四个症状）、Claude TUI 里滚轮过快、文件拖不进终端、中文输入法首个标点要按两次。

**Architecture:** 核心是「窗格记 `sessionId`（稳定），`rootKey` 每次刷新反查」。后端 `ThreadInfo` 增加 `session_ids` 与 `titled` 两个字段，前端加一个纯函数 `resolvePaneIdentity` + 一个 store action `reconcilePanes`，在每次 `refresh()` 后把窗格身份对账回来。滚轮与拖放是两处独立的小改动；输入法是探针，不是设计。

**Tech Stack:** Rust (Tauri 2.11) / TypeScript / React 19 / zustand / vitest / xterm.js 6.0

**Spec:** `docs/superpowers/specs/2026-08-30-aterm-v3-0-design.md`

## Global Constraints

- **本仓库没有 jest-dom。** 断言只用 `toBeTruthy()` / `toBeNull()` / `toBe()` / `classList.contains()`。**禁止** `toBeInTheDocument` / `toHaveClass` / `toHaveValue` / `toHaveTextContent`——这些矩阵器不存在，会直接报错。
- **测试绝不碰真实 `~/.claude`。** Rust 一律 `tempfile::tempdir()`；前端一律 mock `../ipc`。
- **颜色一律取主题 CSS 变量**（`var(--color-*)`），禁止任何硬编码 hex / rgb / hsl / 颜色名。共 28 套主题 × 3 种外观模式。
- **每个测试必须能回答「它会因为什么而失败」。** 断言不出错误行为的测试不算测试，写了等于没写。
- 前端测试命令：`npx vitest run`（**`package.json` 里没有 `test` 脚本**）。单文件：`npx vitest run src/__tests__/<file>`。
- Rust 测试命令：`source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml`
- 类型检查 + 构建：`npm run build`（即 `tsc && vite build`）。
- **基线（开工前实测）**：前端 604 测试 / 39 文件全绿；Rust 109 测试全绿。任何任务结束时这两个数字只能增不能减。
- 受保护文件：`src/components/ConversationPanel.tsx` **不得修改**。`src/components/TerminalView.tsx` 本期**仅** Task 6 允许修改，且改动面严格限定为「常量 → store 订阅」。
- 分支：`feature/v3-0-fixes`（已创建，spec 已提交在其上）。

---

### Task 1: 新对话注入 `--session-id`，并在真机上验证它成立

**这是全计划的地基任务。** spec §2.1 记录了 5 次 shell 探针均因夹具失真（stdin 来自管道而非 pty）而无结论。本任务先做出最小可验证的改动，由**用户在真机 aTerm 里确认**，再决定后续走主路还是 §2.1-fallback。

**Files:**
- Modify: `src/actions.ts`
- Modify: `src/store/tabs.ts`（`Pane` 类型、`openTerminal`、`startPaneTerminal`）
- Modify: `src/components/PanePicker.tsx:59`
- Test: `src/__tests__/actions.test.ts`（新建）

**Interfaces:**
- Produces: `newConversationSpec(cwd: string): { title: string; cwd: string; inject: string; sessionId: string }` — 新对话的唯一构造点。
- Produces: `Pane.sessionId?: string`
- Produces: `openTerminal` / `startPaneTerminal` 的入参对象新增可选 `sessionId?: string`

**背景（实现者必读）：新对话有两个入口，不是一个。**
`HomePage.tsx:131` 走 `newConversation(p.cwd)`；而 `PanePicker.tsx:59` **完全绕过它**，自己写死 `inject: 'claude'`。只改 `actions.ts` 会让窗格选择器里新建的对话依然是坏的。本任务用一个共享构造函数消灭这个分叉。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/actions.test.ts`：

```ts
import { describe, expect, it, vi } from 'vitest'
import { newConversationSpec } from '../actions'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(), ptyWrite: vi.fn(), ptyResize: vi.fn(),
  ptyKill: vi.fn(), ptyIsAlive: vi.fn(), listProjects: vi.fn(),
}))

describe('newConversationSpec', () => {
  it('注入命令里的 session id 与返回的 sessionId 是同一个', () => {
    const spec = newConversationSpec('/tmp/proj')
    expect(spec.inject).toBe(`claude --session-id ${spec.sessionId}`)
  })

  it('生成的是合法 uuid（Rust 侧 is_uuid_stem 会按这个形状校验文件名）', () => {
    const { sessionId } = newConversationSpec('/tmp/proj')
    // is_uuid_stem: 36 字符、4 个短横线、其余全为十六进制
    expect(sessionId.length).toBe(36)
    expect(sessionId.split('-').length - 1).toBe(4)
    expect(/^[0-9a-f-]+$/.test(sessionId)).toBe(true)
  })

  it('每次调用给出不同的 sessionId——同项目并发开两个新对话不能撞车', () => {
    const a = newConversationSpec('/tmp/proj')
    const b = newConversationSpec('/tmp/proj')
    expect(a.sessionId === b.sessionId).toBe(false)
  })

  it('cwd 原样透传，标题固定为「新对话」', () => {
    const spec = newConversationSpec('/tmp/proj')
    expect(spec.cwd).toBe('/tmp/proj')
    expect(spec.title).toBe('新对话')
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/actions.test.ts`
Expected: FAIL —— `newConversationSpec` 尚未导出。

- [ ] **Step 3: 实现**

`src/actions.ts`，替换现有的 `newConversation`：

```ts
// 新对话的唯一构造点。两个入口（主页的「＋ 新对话」、窗格选择器的「新对话」）都必须
// 走它——PanePicker.tsx 此前自己写死 `inject: 'claude'`，绕过了身份绑定，导致窗格
// 选择器里新建的对话同样认不出自己是谁。
//
// 为什么要自己指定 session id：窗格必须在进程启动前就知道自己的身份，否则一切按
// threadKey 认人的逻辑（focusThread 去重、标签标题、对话面板、底栏模型）全部落空。
// 见 spec §2.1 与 §3.1。
export function newConversationSpec(cwd: string): { title: string; cwd: string; inject: string; sessionId: string } {
  const sessionId = crypto.randomUUID()
  return { title: '新对话', cwd, inject: `claude --session-id ${sessionId}`, sessionId }
}

export const newConversation = (cwd: string) =>
  useTabs.getState().openTerminal(newConversationSpec(cwd))
```

`src/store/tabs.ts`：

1. `Pane` 类型加字段（连同注释）：

```ts
// sessionId：只有「我们自己用 --session-id 起的新对话」窗格才有，记录该窗格里跑的
// claude 进程被指定使用的 session id。它是唯一稳定的身份——rootKey 会在首条用户消息
// 出现时从 session_id 翻成那条消息的 uuid（见 scan.rs 的 group_chain_files），所以
// 窗格不能只记 rootKey。--resume 起的窗格不设它：它一开始就知道自己的 rootKey。
export type Pane = { id: string; ptyId?: string; title: string; threadKey?: string; dirName?: string; rootKey?: string; sessionId?: string }
```

2. `openTerminal` 与 `startPaneTerminal` 的入参类型各加 `sessionId?: string`（`TabsState` 里的签名和实现处都要改），并在构造 `Pane` 时透传：

```ts
openTerminal: async ({ title, cwd, inject, threadKey, dirName, rootKey, sessionId }) => {
  await ptyEventsReady
  const ptyId = await ptySpawn({ cwd, inject, cols: 80, rows: 24 })
  const id = `tab-${nextTab++}`
  const pane: Pane = { id: `pane-${nextPane++}`, ptyId, title, threadKey, dirName, rootKey, sessionId }
  set((s) => ({ tabs: [...s.tabs, { id, kind: 'term', title, panes: [pane], activePaneId: pane.id }], activeId: id }))
},
```

`startPaneTerminal` 同样把 `sessionId` 写进它更新的那个 pane 对象。

`src/components/PanePicker.tsx`，把第 59 行改为走同一个构造函数：

```ts
void useTabs.getState().startPaneTerminal(tabId, paneId, newConversationSpec(p.cwd))
```

并在文件顶部 import 里加上 `newConversationSpec`。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/actions.test.ts` → PASS
Run: `npx vitest run` → 全绿，应为 **608**（基线 604 + 本任务 4）
Run: `npm run build` → 无类型错误

- [ ] **Step 5: 提交**

```bash
git add src/actions.ts src/store/tabs.ts src/components/PanePicker.tsx src/__tests__/actions.test.ts
git commit -m "feat(session): 新对话用 --session-id 自带身份，两个入口统一构造

主页与窗格选择器此前各自拼 inject 命令，后者写死 'claude' 绕过了
身份绑定。收敛到 newConversationSpec 一处。"
```

- [ ] **Step 6: 真机验证关卡（必须由用户执行，不可跳过）**

```bash
npm run tauri build
```

请用户：打开新构建的 aTerm → 在主页任一项目上点「＋ 新对话」→ **发一句话** → 然后运行：

```bash
ls -lat ~/.claude/projects/<该项目目录>/*.jsonl | head -3
```

**判定**：最新那个文件名是否就是 aTerm 注入的那个 uuid。（可让用户在窗格里执行 `history` 或直接看终端首行回显的命令来核对 uuid。）

- **通过** → 无需任何改动。
- **不通过** → 按 spec §2.1-fallback 改写本任务的 `newConversationSpec`。**Task 2/4/5 不受影响**——`resolvePaneIdentity` 与 `reconcilePanes` 在两条路上逐字相同。

**（2026-08-30 深夜更新）本关卡已由阻塞降级为验收项。** 原因见 spec §2.1 的"追加的对照实验"：用双向真 pty 复测时，**去掉 `--session-id` 的对照组同样观察不到转录**，说明夹具无法观测转录创建，对这个 flag 没有证言。先前的疑虑已排除，后续任务照常推进。

---

### Task 2: 后端 `ThreadInfo` 增加 `session_ids` 与 `titled`

**Files:**
- Modify: `src-tauri/src/sessions/scan.rs`（结构体定义 + `scan_projects` 内的构造 + tests 模块）

**Interfaces:**
- Produces: `ThreadInfo.session_ids: Vec<String>`（序列化为 `sessionIds`），链上全部文件的 session id，与 `file_count` 同序（时间升序）
- Produces: `ThreadInfo.titled: bool`（序列化为 `titled`），链上是否存在真实标题

**背景：** `scan_projects` 取标题的写法是 `fs.iter().rev().find_map(|fm| fm.meta.title.clone()).unwrap_or_else(|| newest.session_id.chars().take(8).collect())`。新会话尚无标题时 `title` 是 uuid 前 8 位。前端对账器若无条件采纳，标签标题会从「新对话」变成 `ebd067d4`——比不修还糟。给出 `titled` 让前端能分辨，**不要让前端去嗅探字符串**（那等于在两个语言里各留一份同样的回退规则，Rust 一改就静默失效）。

- [ ] **Step 1: 写失败的测试**

在 `src-tauri/src/sessions/scan.rs` 的 `mod tests` 里追加。注意已有的 `write_session` 助手总会写一条 user 消息（因而必然有标题），所以需要另一个只写 assistant 记录的助手：

```rust
    /// 只有 assistant 记录、没有任何 user 消息的会话文件——对应"新对话刚建、用户还
    /// 没发第一句话"。此时 parse_meta 取不到标题，ThreadInfo.titled 必须为 false。
    fn write_untitled_session(dir: &std::path::Path, sid: &str, ts: &str) {
        let l = format!(r#"{{"type":"assistant","uuid":"{sid}-a","timestamp":"{ts}","sessionId":"{sid}","cwd":"/tmp/fake-proj","message":{{"role":"assistant","model":"claude-opus-5"}}}}"#);
        fs::write(dir.join(format!("{sid}.jsonl")), format!("{l}\n")).unwrap();
    }

    #[test]
    fn session_ids_lists_whole_chain_in_time_order() {
        let tmp = tempfile::tempdir().unwrap();
        let proj = tmp.path().join("-tmp-fake-proj");
        fs::create_dir(&proj).unwrap();
        // 同一条链的两个文件：A 早、B 晚
        write_session(&proj, S_A, "u-root", "修复登录 bug", "2026-08-20T10:00:00.000Z");
        write_session(&proj, S_B, "u-root", "修复登录 bug", "2026-08-21T09:00:00.000Z");

        let out = scan_projects(tmp.path());
        let t = &out[0].threads[0];
        assert_eq!(t.session_ids, vec![S_A.to_string(), S_B.to_string()],
            "必须按时间升序列出链上全部 session id——前端靠它把自己指定的 id 映射回当前 root_key");
        assert_eq!(t.session_ids.len() as u32, t.file_count,
            "session_ids 与 file_count 必须同源，否则两者会各自漂移");
    }

    #[test]
    fn titled_false_when_chain_has_no_user_message() {
        let tmp = tempfile::tempdir().unwrap();
        let proj = tmp.path().join("-tmp-fake-proj");
        fs::create_dir(&proj).unwrap();
        write_untitled_session(&proj, S_C, "2026-08-22T10:00:00.000Z");

        let out = scan_projects(tmp.path());
        let t = &out[0].threads[0];
        assert_eq!(t.titled, false, "没有 user 消息就没有真实标题");
        assert_eq!(t.title, S_C.chars().take(8).collect::<String>(),
            "此时 title 是 session_id 前 8 位的回退值——前端必须靠 titled 才能分辨，不能采纳它");
        assert_eq!(t.session_ids, vec![S_C.to_string()]);
    }

    #[test]
    fn titled_true_when_chain_has_a_real_title() {
        let tmp = tempfile::tempdir().unwrap();
        let proj = tmp.path().join("-tmp-fake-proj");
        fs::create_dir(&proj).unwrap();
        write_session(&proj, S_A, "u-root", "修复登录 bug", "2026-08-20T10:00:00.000Z");

        let out = scan_projects(tmp.path());
        let t = &out[0].threads[0];
        assert_eq!(t.titled, true);
        assert_eq!(t.title, "修复登录 bug");
    }
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml`
Expected: 编译失败 —— `ThreadInfo` 没有 `session_ids` / `titled` 字段。

- [ ] **Step 3: 实现**

`ThreadInfo` 结构体末尾加两个字段：

```rust
    /// 本链上所有 jsonl 文件的 session id，按时间升序（与 file_count 同源）。前端的
    /// 窗格对账靠它把"我起进程时用 --session-id 指定的那个 id"映射回当前的 root_key
    /// ——root_key 会在首条用户消息出现时翻一次，不能作为窗格的稳定身份。
    pub session_ids: Vec<String>,
    /// 本链是否已有真实标题。false 表示 `title` 是 session_id 前 8 位的回退值（见下方
    /// scan_projects 里 title 的 unwrap_or_else），前端据此决定要不要采纳它——采纳了
    /// 会把标签标题变成一串 uuid。
    pub titled: bool,
```

`scan_projects` 里，把原来那行 title 拆开并补上两个新字段：

```rust
            let title_opt = fs.iter().rev().find_map(|fm| fm.meta.title.clone());
            let titled = title_opt.is_some();
            let title = title_opt.unwrap_or_else(|| newest.session_id.chars().take(8).collect());
            let session_ids: Vec<String> = fs.iter().map(|fm| fm.session_id.clone()).collect();
```

并在 `ThreadInfo { ... }` 字面量里加 `session_ids,` 与 `titled,`。

不新增任何文件读取——`fs` 里的 `session_id` 是 `group_chain_files` 早就填好的。

- [ ] **Step 4: 运行测试确认通过**

Run: `source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS，总数 112（原 109 + 新增 3）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/sessions/scan.rs
git commit -m "feat(sessions): ThreadInfo 增加 sessionIds 与 titled

sessionIds 供前端把自己指定的 session id 映射回当前 rootKey；
titled 让前端能分辨 title 是真实标题还是 uuid 前 8 位的回退值。"
```

---

### Task 3: 前端类型同步 + `makeThread` 工厂 + 迁移 22 处字面量

**这是一个纯机械任务，不含任何逻辑判断。** 单独成一个提交，不要和对账器逻辑混在一起——否则真正的改动会淹没在 22 处夹具 diff 里，评审看不出重点。

**Files:**
- Modify: `src/ipc.ts`
- Modify: `src/__tests__/factories.ts`
- Modify: 以下 10 个测试文件里的 `ThreadInfo` 字面量（实测共 22 处）：
  `HomePage.test.tsx`(3) · `OverviewPage.test.tsx`(4) · `PanePicker.test.tsx`(4) · `panes.test.tsx`(1) · `SessionBlock.test.tsx`(1) · `sessionSearch.test.ts`(3) · `Sidebar.test.tsx`(1) · `StatusBar.test.tsx`(1) · `statusUI.test.tsx`(2) · `tabs.test.ts`(2)

**Interfaces:**
- Consumes: Task 2 的 `sessionIds` / `titled`
- Produces: `makeThread(overrides?: Partial<ThreadInfo>): ThreadInfo`

- [ ] **Step 1: 改类型，先让编译炸出全部现场**

`src/ipc.ts` 的 `ThreadInfo` 加两个**必填**字段：

```ts
export interface ThreadInfo {
  rootKey: string; resumeSessionId: string; title: string; cwd: string; lastActivityMs: number; fileCount: number
  // 链上全部文件的 session id（时间升序，与 fileCount 同源）。窗格对账用：见
  // paneReconcile.ts。必填——后端恒会给出，写成可选只会把契约的模糊转嫁给调用方。
  sessionIds: string[]
  // title 是否为真实标题。false 时 title 是 resumeSessionId 前 8 位的回退值，不可采纳。
  titled: boolean
  // 徽章数据：均可缺省（老会话或异常记录取不到时为 null），与 rust 端 ThreadInfo 同源。
  model?: string | null
  contextTokens?: number | null
  preview?: string | null
  effort?: string | null
  permissionMode?: string | null
}
```

- [ ] **Step 2: 运行类型检查，取得完整的待修清单**

Run: `npm run build`
Expected: FAIL，列出全部 22 处缺字段的字面量。**照这份清单改，不要凭记忆改。**

- [ ] **Step 3: 加工厂**

`src/__tests__/factories.ts` 追加（并在顶部 import 里加 `import type { ThreadInfo } from '../ipc'`）：

```ts
export function makeThread(overrides: Partial<ThreadInfo> = {}): ThreadInfo {
  seq += 1
  return {
    rootKey: `r-f${seq}`,
    resumeSessionId: `s-f${seq}`,
    title: `会话${seq}`,
    cwd: '/tmp/proj',
    lastActivityMs: seq,
    fileCount: 1,
    sessionIds: [`s-f${seq}`],
    titled: true,
    ...overrides,
  }
}
```

同时更新该文件顶部那段注释：原文说"只用于本次新增的用例，不回头改写已通过的旧测试字面量"，该约定的前提是"旧字面量不受影响"，现在不成立了——改为说明 `ThreadInfo` 字面量已统一走 `makeThread`，此后新增字段只需改这一处。

- [ ] **Step 4: 迁移 22 处字面量**

逐个把 `{ rootKey: 'r1', resumeSessionId: 's1', ... }` 换成 `makeThread({ rootKey: 'r1', ... })`，**只保留该用例真正关心的字段**作为 overrides，其余交给工厂默认值。已有本地 `thread()` 助手的两个文件（`OverviewPage.test.tsx`、`StatusBar.test.tsx`）把助手实现改为委托：

```ts
const thread = (over: Partial<ThreadInfo> = {}) => makeThread(over)
```

**注意**：`sessionSearch.test.ts` 断言的是标题匹配，迁移时务必保留原有的 `title` 值；`makeThread` 的默认标题是 `会话N`，会让搜索用例失去意义。

- [ ] **Step 5: 全量验证**

Run: `npm run build` → 无类型错误
Run: `npx vitest run` → 应为 **608**（本任务**不新增**测试，数量与 Task 1 结束时一致；若有测试变红，说明迁移时把某个用例关心的字段丢了）

- [ ] **Step 6: 提交**

```bash
git add src/ipc.ts src/__tests__/
git commit -m "refactor(test): ThreadInfo 字面量统一走 makeThread 工厂

ipc.ts 的 ThreadInfo 增加必填 sessionIds/titled，10 个测试文件里的
22 处字面量随之迁移到工厂。此后新增字段只需改工厂一处。"
```

---

### Task 4: `resolvePaneIdentity` 纯函数

**Files:**
- Create: `src/paneReconcile.ts`
- Test: `src/__tests__/paneReconcile.test.ts`

**Interfaces:**
- Consumes: `ProjectInfo` / `ThreadInfo`（`src/ipc.ts`），`makeThread`（`src/__tests__/factories.ts`）
- Produces:
  ```ts
  export type PaneIdentity = { dirName: string; rootKey: string; threadKey: string; title?: string }
  export function resolvePaneIdentity(projects: ProjectInfo[], sessionId: string): PaneIdentity | null
  ```

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/paneReconcile.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import type { ProjectInfo } from '../ipc'
import { resolvePaneIdentity } from '../paneReconcile'
import { makeThread } from './factories'

const project = (dirName: string, threads: ProjectInfo['threads']): ProjectInfo =>
  ({ dirName, cwd: `/tmp/${dirName}`, lastActivityMs: 1, threads })

describe('resolvePaneIdentity', () => {
  it('命中链上任意一个文件的 session id，都解析到该链当前的 rootKey', () => {
    const projects = [project('-tmp-a', [
      makeThread({ rootKey: 'u-root', sessionIds: ['s-old', 's-new'], titled: true, title: '修登录' }),
    ])]
    // 用链上较早的那个 id 去查——这正是新对话的真实处境：我们指定的 id 是链的起点，
    // 而 rootKey 在用户发出第一句话后已经翻成了那条消息的 uuid。
    const got = resolvePaneIdentity(projects, 's-old')
    expect(got?.rootKey).toBe('u-root')
    expect(got?.dirName).toBe('-tmp-a')
    expect(got?.threadKey).toBe('-tmp-a:u-root')
    expect(got?.title).toBe('修登录')
  })

  it('titled 为 false 时不给 title——采纳回退值会把标签标题变成一串 uuid', () => {
    const projects = [project('-tmp-a', [
      makeThread({ rootKey: 's-fresh', sessionIds: ['s-fresh'], titled: false, title: 's-fresh'.slice(0, 8) }),
    ])]
    const got = resolvePaneIdentity(projects, 's-fresh')
    expect(got).not.toBeNull()
    expect(got!.rootKey).toBe('s-fresh')
    expect(got!.title).toBe(undefined)
  })

  it('转录尚未落盘时返回 null，调用方据此保留窗格原样', () => {
    const projects = [project('-tmp-a', [makeThread({ sessionIds: ['s-other'] })])]
    expect(resolvePaneIdentity(projects, 's-mine')).toBeNull()
  })

  it('跨项目出现相同 rootKey 时不误配——threadKey 必须带项目前缀', () => {
    const projects = [
      project('-tmp-a', [makeThread({ rootKey: 'same', sessionIds: ['s-a'], titled: true, title: 'A' })]),
      project('-tmp-b', [makeThread({ rootKey: 'same', sessionIds: ['s-b'], titled: true, title: 'B' })]),
    ]
    expect(resolvePaneIdentity(projects, 's-b')!.threadKey).toBe('-tmp-b:same')
    expect(resolvePaneIdentity(projects, 's-a')!.threadKey).toBe('-tmp-a:same')
  })

  it('projects 为空时返回 null，不抛异常', () => {
    expect(resolvePaneIdentity([], 's-mine')).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/paneReconcile.test.ts`
Expected: FAIL —— 模块 `../paneReconcile` 不存在。

- [ ] **Step 3: 实现**

新建 `src/paneReconcile.ts`：

```ts
// 窗格身份对账（spec §3）：窗格记 sessionId（我们用 --session-id 指定、永不变），
// rootKey 每次刷新反查得出。
//
// 为什么不能让窗格直接记 rootKey：scan.rs 的 group_chain_files 用"首条用户消息 uuid，
// 缺失时退回自身 session_id"作链键。新对话刚建时 rootKey == 我们给的 session_id，
// 用户发出第一句话后它翻成那条消息的 uuid——只记 rootKey 的窗格会绑对一瞬间，随即
// 再次失联。
import type { ProjectInfo } from './ipc'

export type PaneIdentity = { dirName: string; rootKey: string; threadKey: string; title?: string }

/** 在 projects 里按 sessionId 找到它当前所属的链，算出该窗格此刻应有的身份。
 *  找不到（转录尚未落盘、或用户在窗格里退出 claude 后跑了别的命令）返回 null，
 *  调用方保持窗格原样，不清空已有身份。 */
export function resolvePaneIdentity(projects: ProjectInfo[], sessionId: string): PaneIdentity | null {
  for (const project of projects) {
    for (const thread of project.threads) {
      if (!thread.sessionIds.includes(sessionId)) continue
      return {
        dirName: project.dirName,
        rootKey: thread.rootKey,
        // 与 actions.ts 的 resumeThread 逐字相同的拼法。rootKey 只在单个项目目录内
        // 唯一（见 scan.rs 按目录分组），跨项目必须以「项目:会话」复合键去重。
        threadKey: `${project.dirName}:${thread.rootKey}`,
        // 只在有真实标题时给出。titled 为 false 时 thread.title 是 session_id 前 8 位
        // 的回退值（见 scan.rs），采纳它会把标签标题从「新对话」变成一串 uuid。
        title: thread.titled ? thread.title : undefined,
      }
    }
  }
  return null
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/paneReconcile.test.ts` → PASS（5 个用例）
Run: `npx vitest run` → 应为 **613**（608 + 本任务 5）

- [ ] **Step 5: 提交**

```bash
git add src/paneReconcile.ts src/__tests__/paneReconcile.test.ts
git commit -m "feat(session): 加 resolvePaneIdentity——按 sessionId 反查窗格当前身份"
```

---

### Task 5: `reconcilePanes` store action + App.tsx 接线

**Files:**
- Modify: `src/store/tabs.ts`
- Modify: `src/App.tsx`
- Test: `src/__tests__/paneReconcileStore.test.ts`（新建）

**Interfaces:**
- Consumes: `resolvePaneIdentity`（Task 4）、`Pane.sessionId`（Task 1）
- Produces: `useTabs.getState().reconcilePanes(projects: ProjectInfo[]): void`

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/paneReconcileStore.test.ts`：

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import type { ProjectInfo } from '../ipc'
import { useTabs } from '../store/tabs'
import { makeThread } from './factories'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(), ptyWrite: vi.fn(), ptyResize: vi.fn(),
  ptyKill: vi.fn(), ptyIsAlive: vi.fn().mockResolvedValue(false), listProjects: vi.fn(),
}))

const projects = (threads: ProjectInfo['threads']): ProjectInfo[] =>
  [{ dirName: '-tmp-a', cwd: '/tmp/a', lastActivityMs: 1, threads }]

beforeEach(() => {
  useTabs.setState({
    tabs: [
      { id: 'home', kind: 'home', title: '主页', panes: [] },
      {
        id: 'tab-1', kind: 'term', title: '新对话', activePaneId: 'pane-1',
        panes: [{ id: 'pane-1', ptyId: 'pty-1', title: '新对话', sessionId: 's-mine' }],
      },
    ],
    activeId: 'tab-1',
  })
})

describe('reconcilePanes', () => {
  it('回填 dirName / rootKey / threadKey，使 focusThread 能命中', () => {
    useTabs.getState().reconcilePanes(projects([
      makeThread({ rootKey: 'u-root', sessionIds: ['s-mine'], titled: true, title: '修登录' }),
    ]))
    const pane = useTabs.getState().tabs[1].panes[0]
    expect(pane.dirName).toBe('-tmp-a')
    expect(pane.rootKey).toBe('u-root')
    expect(pane.threadKey).toBe('-tmp-a:u-root')
    // 这才是用户真正感知到的症状：从侧栏点同一条会话，应聚焦到已有窗格而不是再开一个
    expect(useTabs.getState().focusThread('-tmp-a:u-root')).toBe(true)
  })

  it('有真实标题时同步窗格标题与标签标题', () => {
    useTabs.getState().reconcilePanes(projects([
      makeThread({ rootKey: 'u-root', sessionIds: ['s-mine'], titled: true, title: '修登录' }),
    ]))
    expect(useTabs.getState().tabs[1].panes[0].title).toBe('修登录')
    expect(useTabs.getState().tabs[1].title).toBe('修登录')
  })

  it('titled 为 false 时保留「新对话」，不显示 uuid 前 8 位', () => {
    useTabs.getState().reconcilePanes(projects([
      makeThread({ rootKey: 's-mine', sessionIds: ['s-mine'], titled: false, title: 's-mine12' }),
    ]))
    expect(useTabs.getState().tabs[1].panes[0].title).toBe('新对话')
    expect(useTabs.getState().tabs[1].title).toBe('新对话')
    // 身份仍然要绑上——只是标题不采纳
    expect(useTabs.getState().tabs[1].panes[0].rootKey).toBe('s-mine')
  })

  it('身份未变时返回同一个 tabs 引用，不制造无谓的重渲染', () => {
    const p = projects([makeThread({ rootKey: 'u-root', sessionIds: ['s-mine'], titled: true, title: '修登录' })])
    useTabs.getState().reconcilePanes(p)
    const first = useTabs.getState().tabs
    useTabs.getState().reconcilePanes(p)
    expect(useTabs.getState().tabs).toBe(first)
  })

  it('转录尚未落盘时不动窗格，也不清空已有字段', () => {
    useTabs.getState().reconcilePanes(projects([makeThread({ sessionIds: ['s-other'] })]))
    const pane = useTabs.getState().tabs[1].panes[0]
    expect(pane.rootKey).toBe(undefined)
    expect(pane.title).toBe('新对话')
  })

  it('没有 sessionId 的窗格（--resume 起的）完全不受影响', () => {
    useTabs.setState({
      tabs: [
        { id: 'home', kind: 'home', title: '主页', panes: [] },
        {
          id: 'tab-1', kind: 'term', title: '旧会话', activePaneId: 'pane-1',
          panes: [{ id: 'pane-1', ptyId: 'pty-1', title: '旧会话', dirName: '-tmp-a', rootKey: 'u-old', threadKey: '-tmp-a:u-old' }],
        },
      ],
      activeId: 'tab-1',
    })
    const before = useTabs.getState().tabs
    useTabs.getState().reconcilePanes(projects([makeThread({ rootKey: 'u-root', sessionIds: ['s-mine'] })]))
    expect(useTabs.getState().tabs).toBe(before)
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/paneReconcileStore.test.ts`
Expected: FAIL —— `reconcilePanes is not a function`。

- [ ] **Step 3: 实现**

`src/store/tabs.ts`：顶部 import 加 `import { resolvePaneIdentity } from '../paneReconcile'` 与 `import type { ProjectInfo } from '../ipc'`；`TabsState` 加签名 `reconcilePanes(projects: ProjectInfo[]): void`；实现：

```ts
  // 窗格身份对账（spec §3.3c）：把"我起进程时指定的 sessionId"映射回该链此刻的
  // rootKey/标题，回填进窗格。一处对账同时修好四个症状：标签标题不更新、侧栏点击
  // 又开新标签、右侧对话面板不加载、底栏模型不显示。
  //
  // 只处理 sessionId 非空的窗格——--resume 起的窗格一开始就知道自己的 rootKey，
  // 不需要也不该被这里改动。
  //
  // 身份完全未变时返回同一个 tabs 引用（no-op，不产生新对象），与本文件既有惯例
  // 一致（moveArrayItem、movePanesToTab 拖到自己标签时都是这样）——这个 action 每
  // 15 秒被调用一次，制造新引用会让整棵标签树无谓重渲染。
  reconcilePanes: (projects) => {
    set((s) => {
      let changed = false
      const tabs = s.tabs.map((tab) => {
        if (tab.kind !== 'term') return tab
        let tabChanged = false
        const panes = tab.panes.map((pane) => {
          if (!pane.sessionId) return pane
          const id = resolvePaneIdentity(projects, pane.sessionId)
          if (!id) return pane
          // title 只在 resolvePaneIdentity 给出时才采纳（titled 为真）；否则保留窗格
          // 现有标题（「新对话」），不要写入 uuid 前 8 位的回退值。
          const nextTitle = id.title ?? pane.title
          if (
            pane.dirName === id.dirName && pane.rootKey === id.rootKey &&
            pane.threadKey === id.threadKey && pane.title === nextTitle
          ) return pane
          tabChanged = true
          return { ...pane, dirName: id.dirName, rootKey: id.rootKey, threadKey: id.threadKey, title: nextTitle }
        })
        if (!tabChanged) return tab
        changed = true
        return { ...tab, panes, title: deriveTabTitle(panes, tab.title) }
      })
      return changed ? { tabs } : {}
    })
  },
```

`src/App.tsx`：把三个刷新触发点统一收敛到一个"刷新并对账"的函数。在组件顶部（`refresh` 之后）加：

```ts
  // refresh() 有三个触发点（挂载、window focus、statusVersion 变化时的节流刷新），
  // 每一处刷新之后都必须对账——否则新对话的身份会在其中某些路径上永远补不上。
  // 不把对账塞进 useSessions.refresh 内部：那会让一个纯数据 store 反向依赖 tabs
  // 这个 UI 状态 store。
  const refreshAndReconcile = useCallback(async () => {
    await refresh()
    useTabs.getState().reconcilePanes(useSessions.getState().projects)
  }, [refresh])
```

然后把原 effect 里的三处 `refresh()` 全部换成 `refreshAndReconcile()`，并把该 effect 的依赖数组从 `[refresh]` 改为 `[refreshAndReconcile]`。记得从 `react` import 里补上 `useCallback`。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/paneReconcileStore.test.ts` → PASS（6 个用例）
Run: `npx vitest run` → 应为 **619**（613 + 本任务 6）
Run: `npm run build` → 无类型错误

- [ ] **Step 5: 提交**

```bash
git add src/store/tabs.ts src/App.tsx src/__tests__/paneReconcileStore.test.ts
git commit -m "fix(session): 新对话身份对账，修好标签标题/侧栏去重/对话面板/底栏模型

四个症状同一根因：newConversation 不传 threadKey，一切按 threadKey
认人的逻辑全部落空。每次 refresh 后按 sessionId 反查回填即可。"
```

- [ ] **Step 6: 真机验收（由用户执行）**

`npm run tauri build` 后，请用户依次确认：

1. 主页点「＋ 新对话」→ 说一句话 → **上方标签标题从「新对话」变成真实标题**（最多等 15 秒，那是元数据刷新的节流间隔）
2. 此时点左侧「最近会话」里的同一条 → **聚焦到已有标签，不再新开一个**
3. 右侧对话面板（⌘J）→ **能加载出这条新对话的内容**
4. 底部状态栏 → **显示模型 · effort · 权限模式**，不再空白
5. 用 ⌘D 分屏，在窗格选择器里点「新对话」→ **同样满足以上四条**（这条专门验 PanePicker 那个曾经绕过身份绑定的入口）

---

### Task 6: 滚轮倍率 3 → 1.5

**Files:**
- Modify: `src/wheel.ts`
- Modify: `src/store/layout.ts`
- Modify: `src/components/TerminalView.tsx`（**受保护文件，本任务已获用户明确许可，改动面严格限定为「常量 → store 订阅」**）
- Test: `src/__tests__/wheel.test.ts`、`src/__tests__/layout.test.ts`

**Interfaces:**
- Produces: `useLayout` 增加 `wheelMultiplier: number` 与 `setWheelMultiplier(n: number): void`

**背景：** 现有 `createWheelAmplifier` 的循环是 `for (let i = 1; i < multiplier; i++)`，传 1.5 会补发 1 个合成事件 → 实际 2 倍，不是 1.5 倍。必须改成带余量的累加器。范围**仅限** Claude TUI 自己接管鼠标上报时走的那条分支（`ALT_WHEEL_MOUSE_MULTIPLIER`）；另两个旋钮（`scrollSensitivity: 5` 的普通回滚、右侧面板的 DOM 滚动）用户已明确要求**不动**。

- [ ] **Step 1: 写失败的测试**

`src/__tests__/wheel.test.ts` 的 `describe('createWheelAmplifier')` 里追加：

```ts
  it('小数倍率靠余量累积逼近——1.5 倍在 10 次真实事件后总补发 5 次', () => {
    const target = document.createElement('div')
    const amplify = createWheelAmplifier(1.5)
    let invocationCount = 0
    target.addEventListener('wheel', (e) => {
      invocationCount++
      amplify(target, e as WheelEvent)
    })
    for (let i = 0; i < 10; i++) {
      target.dispatchEvent(new WheelEvent('wheel', { deltaY: 100, bubbles: true, cancelable: true }))
    }
    // 10 次真实 + 5 次补发 = 15。改动前的实现会补发 10 次（每次都进一次循环体），
    // 得到 20——这个用例正是用来钉住那个差别的。
    expect(invocationCount).toBe(15)
  })

  it('小数倍率在单次事件上不四舍五入——第一次不补发，第二次才补发', () => {
    const target = document.createElement('div')
    const amplify = createWheelAmplifier(1.5)
    let invocationCount = 0
    target.addEventListener('wheel', (e) => {
      invocationCount++
      amplify(target, e as WheelEvent)
    })
    target.dispatchEvent(new WheelEvent('wheel', { deltaY: 100, bubbles: true, cancelable: true }))
    expect(invocationCount).toBe(1)
    target.dispatchEvent(new WheelEvent('wheel', { deltaY: 100, bubbles: true, cancelable: true }))
    expect(invocationCount).toBe(3) // 第二次真实事件 + 它带出的 1 次补发
  })
```

`src/__tests__/layout.test.ts` 追加一个新的 describe。**必须沿用该文件既有的写法**——持久化读取发生在模块求值时，所以每个用例都要先 mock localStorage 再**动态 import** store（文件顶部已有 `vi.resetModules()` 的 `beforeEach` 与 `mockLocalStorage` 助手，直接复用，不要新写一套）：

```ts
describe('layout store — wheelMultiplier', () => {
  it('首次启动、本地无偏好时默认 1.5', async () => {
    mockLocalStorage()
    const { useLayout } = await import('../store/layout')
    expect(useLayout.getState().wheelMultiplier).toBe(1.5)
  })

  it('已持久化的值优先于默认值', async () => {
    mockLocalStorage({ 'aterm-wheel-multiplier': '2.5' })
    const { useLayout } = await import('../store/layout')
    expect(useLayout.getState().wheelMultiplier).toBe(2.5)
  })

  it('setWheelMultiplier 钳制到 [1, 6] 并持久化', async () => {
    const ls = mockLocalStorage()
    const { useLayout } = await import('../store/layout')
    useLayout.getState().setWheelMultiplier(0.2)
    expect(useLayout.getState().wheelMultiplier).toBe(1)
    expect(ls.setItem).toHaveBeenCalledWith('aterm-wheel-multiplier', '1')
    useLayout.getState().setWheelMultiplier(99)
    expect(useLayout.getState().wheelMultiplier).toBe(6)
    useLayout.getState().setWheelMultiplier(2.5)
    expect(useLayout.getState().wheelMultiplier).toBe(2.5)
  })

  it('持久化的值是坏数据时退回默认值，不产生 NaN 倍率', async () => {
    // NaN 倍率会让 createWheelAmplifier 的 carry 变成 NaN，从此再也不补发任何事件——
    // 表现为"滚轮突然完全失去加速"，且没有任何报错。
    mockLocalStorage({ 'aterm-wheel-multiplier': 'abc' })
    const { useLayout } = await import('../store/layout')
    expect(useLayout.getState().wheelMultiplier).toBe(1.5)
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/wheel.test.ts src/__tests__/layout.test.ts`
Expected: wheel 两个新用例 FAIL（得到 20 和 2，而非 15 和 3）；layout 四个新用例 FAIL（`wheelMultiplier` 不存在）。

- [ ] **Step 3: 实现**

`src/wheel.ts` 的 `createWheelAmplifier`，把整数循环换成带余量的累加器（保留原有的全部注释，只改函数体，并补一段说明小数倍率）：

```ts
export function createWheelAmplifier(multiplier: number): (target: EventTarget, ev: WheelEvent) => void {
  let synthesizing = false
  // 未满一个事件的补发余量，跨事件累加。合成事件只能是整数个，而 1.5 这样的倍率
  // 要求"平均每次补发 0.5 个"——靠余量累积实现：攒够 1 才真的补发一个，长期均值
  // 收敛到 multiplier。与 wheelDeltaToLines 的 remainder 是同一手法。
  // 整数倍率下 carry 恒为 0，行为与改动前逐次相同。
  let carry = 0
  return (target, ev) => {
    if (synthesizing) return
    carry += multiplier - 1
    const extra = Math.floor(carry)
    carry -= extra
    if (extra <= 0) return
    synthesizing = true
    try {
      for (let i = 0; i < extra; i++) {
        target.dispatchEvent(new WheelEvent('wheel', {
          deltaX: ev.deltaX, deltaY: ev.deltaY, deltaZ: ev.deltaZ, deltaMode: ev.deltaMode,
          clientX: ev.clientX, clientY: ev.clientY,
          bubbles: true, cancelable: true, view: ev.view,
        }))
      }
    } finally {
      synthesizing = false
    }
  }
}
```

`src/store/layout.ts`：在 `LayoutState` 加 `wheelMultiplier: number` 与 `setWheelMultiplier(n: number): void`；加常量与读写函数，沿用 `readPersistedFontSize` 那套「`v !== null` 显式区分未存过与存过假值」的模式：

```ts
const WHEEL_MULTIPLIER_KEY = 'aterm-wheel-multiplier'
// Claude TUI 自己接管鼠标上报时，每个真实滚轮事件的放大倍数。原为 3，用户反馈过快。
// 滑块 UI 留到 V3.2（届时会有设置入口）；值先进 store 是为了那时的滑块是纯 UI 增量。
export const WHEEL_MULTIPLIER_DEFAULT = 1.5
const WHEEL_MULTIPLIER_MIN = 1
const WHEEL_MULTIPLIER_MAX = 6

function clampWheelMultiplier(n: number): number {
  if (!Number.isFinite(n)) return WHEEL_MULTIPLIER_DEFAULT
  return Math.min(WHEEL_MULTIPLIER_MAX, Math.max(WHEEL_MULTIPLIER_MIN, n))
}

function readPersistedWheelMultiplier(): number {
  try {
    const v = localStorage.getItem(WHEEL_MULTIPLIER_KEY)
    if (v !== null) return clampWheelMultiplier(Number(v))
  } catch { /* localStorage 不可用（如隐私模式），忽略 */ }
  return WHEEL_MULTIPLIER_DEFAULT
}
```

store 里：`wheelMultiplier: readPersistedWheelMultiplier(),` 与

```ts
  setWheelMultiplier: (n) => {
    const v = clampWheelMultiplier(n)
    try { localStorage.setItem(WHEEL_MULTIPLIER_KEY, String(v)) } catch { /* 忽略持久化失败 */ }
    set({ wheelMultiplier: v })
  },
```

`src/components/TerminalView.tsx`：**只改这两处**——

1. 删掉 `const ALT_WHEEL_MOUSE_MULTIPLIER = 3`（保留 `ALT_WHEEL_MULTIPLIER = 3`，那是另一条分支，本期不动）。
2. 把 `const amplifyMouseWheel = createWheelAmplifier(ALT_WHEEL_MOUSE_MULTIPLIER)` 改为从 store 取值：

```ts
    const amplifyMouseWheel = createWheelAmplifier(useLayout.getState().wheelMultiplier)
```

3. 诊断行（⌘⇧D）里引用了 `ALT_WHEEL_MOUSE_MULTIPLIER` 的那处改为 `useLayout.getState().wheelMultiplier`。

**不要**为它加订阅：倍率改变后对已开终端不立即生效是可接受的（V3.2 加滑块时再一并处理），而在这个 effect 里多挂一个 `useLayout.subscribe` 会增加受保护文件的改动面。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/wheel.test.ts src/__tests__/layout.test.ts` → PASS
Run: `npx vitest run` → 应为 **625**（619 + 本任务 6）全绿（既有的 `multiplier=4` 与 `multiplier=1` 两个用例必须仍然通过——它们是整数倍率不回归的证据）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/wheel.ts src/store/layout.ts src/components/TerminalView.tsx src/__tests__/wheel.test.ts src/__tests__/layout.test.ts
git commit -m "fix(wheel): Claude TUI 滚轮倍率 3 -> 1.5，改用余量累加器

原实现的 for (i=1; i<multiplier) 传 1.5 会补发 1 个，实际是 2 倍。
倍率移入 layout store 持久化，滑块 UI 留到 V3.2。"
```

---

### Task 7: 拖放的三个纯函数

**Files:**
- Create: `src/fileDrop.ts`
- Test: `src/__tests__/fileDrop.test.ts`

**Interfaces:**
- Consumes: `PaneSlotRect`（`src/paneDrop.ts`）
- Produces:
  ```ts
  export function shellQuote(path: string): string
  export function formatDroppedPaths(paths: string[]): string
  export function paneAtPoint(rects: PaneSlotRect[], x: number, y: number): string | null
  export function toLogicalPoint(p: { x: number; y: number }, dpr: number): { x: number; y: number }
  ```

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/fileDrop.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { formatDroppedPaths, paneAtPoint, shellQuote, toLogicalPoint } from '../fileDrop'

describe('shellQuote', () => {
  it('普通路径被单引号包裹', () => {
    expect(shellQuote('/tmp/a.txt')).toBe("'/tmp/a.txt'")
  })
  it('含空格的路径不会被 shell 拆成两个参数', () => {
    expect(shellQuote('/tmp/my file.txt')).toBe("'/tmp/my file.txt'")
  })
  it('含单引号的路径被正确断开重接——这是唯一能逃逸出引号的字符', () => {
    // it's.txt -> 'it'\''s.txt'
    expect(shellQuote("/tmp/it's.txt")).toBe("'/tmp/it'\\''s.txt'")
  })
  it('中文路径原样保留在引号内', () => {
    expect(shellQuote('/tmp/项目/说明.md')).toBe("'/tmp/项目/说明.md'")
  })
  it('含 $ 与反引号的路径不会被展开——单引号内一切都是字面量', () => {
    expect(shellQuote('/tmp/$HOME`whoami`.txt')).toBe("'/tmp/$HOME`whoami`.txt'")
  })
})

describe('formatDroppedPaths', () => {
  it('多个路径以空格分隔，并以一个空格结尾便于继续打字', () => {
    expect(formatDroppedPaths(['/a', '/b'])).toBe("'/a' '/b' ")
  })
  it('空数组返回空串，调用方据此不写入任何内容', () => {
    expect(formatDroppedPaths([])).toBe('')
  })
})

describe('toLogicalPoint', () => {
  it('Retina 上把物理像素换算成 CSS 像素', () => {
    // 这是本模块唯一会"在 Retina 上错、在普通屏上对"的失效模式——必须钉住。
    expect(toLogicalPoint({ x: 800, y: 600 }, 2)).toEqual({ x: 400, y: 300 })
  })
  it('dpr 为 1 时原样返回', () => {
    expect(toLogicalPoint({ x: 800, y: 600 }, 1)).toEqual({ x: 800, y: 600 })
  })
})

describe('paneAtPoint', () => {
  const rects = [
    { paneId: 'p1', rect: { top: 100, left: 0, width: 200, height: 400 } },
    { paneId: 'p2', rect: { top: 100, left: 200, width: 200, height: 400 } },
  ]
  it('落在第二个窗格里就返回它，而不是当前聚焦的那个', () => {
    expect(paneAtPoint(rects, 300, 200)).toBe('p2')
  })
  it('落在第一个窗格里', () => {
    expect(paneAtPoint(rects, 50, 200)).toBe('p1')
  })
  it('落在窗格区之外（如标签栏）返回 null，调用方据此整个忽略', () => {
    expect(paneAtPoint(rects, 300, 50)).toBeNull()
  })
  it('边界：左上角含、右下角不含，相邻窗格不会同时命中', () => {
    expect(paneAtPoint(rects, 200, 100)).toBe('p2')
    expect(paneAtPoint(rects, 400, 200)).toBeNull()
  })
  it('没有任何窗格时返回 null', () => {
    expect(paneAtPoint([], 10, 10)).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/fileDrop.test.ts`
Expected: FAIL —— 模块 `../fileDrop` 不存在。

- [ ] **Step 3: 实现**

新建 `src/fileDrop.ts`：

```ts
// 把文件拖进终端（spec §5）用到的纯函数。DOM/Tauri 事件的接线在 App.tsx，
// 这里只放可单测的换算与字符串处理。
import type { PaneSlotRect } from './paneDrop'

/** 单引号包裹；内部的 ' 用 '\'' 断开重接。这是 POSIX shell 里唯一无需枚举元字符的
 *  安全写法——单引号内除 ' 本身之外一切字符都是字面量，$ / 反引号 / 空格 / 换行全部
 *  失去特殊含义，因此不需要维护一张"危险字符"清单（那种清单迟早会漏）。 */
export function shellQuote(path: string): string {
  return `'${path.split("'").join("'\\''")}'`
}

/** 多个文件以空格分隔；末尾补一个空格，便于用户接着打字。空数组返回空串。 */
export function formatDroppedPaths(paths: string[]): string {
  if (paths.length === 0) return ''
  return paths.map(shellQuote).join(' ') + ' '
}

/** Tauri 的拖放坐标是 PhysicalPosition（物理像素），而 getBoundingClientRect() 是
 *  CSS 像素。Retina 上 devicePixelRatio = 2，不换算会整体偏一倍——而在外接的非
 *  Retina 显示器上又恰好正确，是最难查的那类缺陷。 */
export function toLogicalPoint(p: { x: number; y: number }, dpr: number): { x: number; y: number } {
  return { x: p.x / dpr, y: p.y / dpr }
}

/** 命中测试：左上闭、右下开，保证相邻窗格的公共边只归属右/下那个，不会同时命中。 */
export function paneAtPoint(rects: PaneSlotRect[], x: number, y: number): string | null {
  for (const { paneId, rect } of rects) {
    if (x >= rect.left && x < rect.left + rect.width && y >= rect.top && y < rect.top + rect.height) {
      return paneId
    }
  }
  return null
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/fileDrop.test.ts` → PASS（14 个用例）
Run: `npx vitest run` → 应为 **639**（625 + 本任务 14）

- [ ] **Step 5: 提交**

```bash
git add src/fileDrop.ts src/__tests__/fileDrop.test.ts
git commit -m "feat(drop): 加文件拖放的纯函数——shell 转义、DPR 换算、窗格命中"
```

---

### Task 8: 拖放接线 + 坐标原点验证 + 落点高亮

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.css`
- Modify: `src/components/TabPanes.tsx`（给窗格加落点高亮 class）

**Interfaces:**
- Consumes: Task 7 的四个纯函数、`getPaneSlotRects`（`src/paneDropDom.ts`）、`ptyWrite`（`src/ipc.ts`）

**背景：** Tauri 2 默认接管原生拖放，webview 里的 HTML5 `drop` 根本不触发——这正是"拖进去什么都没有"的原因。不要去关 `dragDropEnabled`；Tauri 交付的 `paths` 是真实文件系统路径，比 HTML5 DataTransfer 能拿到的更可靠。

事件是**可辨识联合，四种形态字段各不相同**（已对照 `@tauri-apps/api` 2.11.1 的 `webview.d.ts`）：

```ts
| { type: 'enter';  paths: string[]; position: PhysicalPosition }
| { type: 'over';                    position: PhysicalPosition }   // 无 paths
| { type: 'drop';   paths: string[]; position: PhysicalPosition }
| { type: 'leave' }                                                 // 连 position 都没有
```

必须按 `type` 收窄后再取字段。

- [ ] **Step 1: 先验证坐标原点（写代码之前做）**

在 `App.tsx` 里临时加一个监听，打印换算后的坐标与某个已知窗格的矩形：

```ts
  useEffect(() => {
    let un: (() => void) | undefined
    void import('@tauri-apps/api/webview').then(({ getCurrentWebview }) =>
      getCurrentWebview().onDragDropEvent((e) => {
        if (e.payload.type !== 'over') return
        const p = toLogicalPoint(e.payload.position, window.devicePixelRatio)
        console.log('[drop-probe]', p, getPaneSlotRects(useTabs.getState().tabs.find((t) => t.id === useTabs.getState().activeId)))
      }).then((f) => { un = f }),
    )
    return () => un?.()
  }, [])
```

`npm run tauri build` 后，请用户开一个终端标签、从 Finder 拖一个文件**悬停在窗格正中央**，把控制台输出（⌘⌥I 打开 devtools）回传。

**判定**：换算后的坐标是否落在该窗格矩形内。若存在固定偏移（例如恰好等于标题栏高度），把偏移量减掉并写进注释说明来源。

**不要靠肉眼觉得"差不多能用"就过**——偏移量恰好接近标题栏高度时，拖到窗格上半部**仍会命中**，只有下边缘露馅。

- [ ] **Step 2: 删掉探针，写正式实现**

`src/App.tsx`（在既有的 effect 之后新增一个）：

```ts
  // 文件拖放（spec §5）：Tauri 接管了原生拖放，webview 的 HTML5 drop 不会触发，
  // 必须走 onDragDropEvent。它给的是真实文件系统路径，比 DataTransfer 更可靠。
  // 落点写进"光标底下那个窗格"而非当前聚焦窗格——分屏下用户明确瞄准了某一个。
  useEffect(() => {
    let unlisten: (() => void) | undefined
    let disposed = false
    void import('@tauri-apps/api/webview').then(async ({ getCurrentWebview }) => {
      const un = await getCurrentWebview().onDragDropEvent((e) => {
        const payload = e.payload
        if (payload.type === 'leave') { setDropPaneId(null); return }
        // 'enter' / 'over' / 'drop' 三者都带 position；只有 enter/drop 带 paths。
        const { x, y } = toLogicalPoint(payload.position, window.devicePixelRatio)
        const { tabs, activeId } = useTabs.getState()
        const tab = tabs.find((t) => t.id === activeId)
        const paneId = paneAtPoint(getPaneSlotRects(tab), x, y)
        if (payload.type !== 'drop') { setDropPaneId(paneId); return }
        setDropPaneId(null)
        if (!paneId) return // 落在窗格区之外（主页/总览/标签栏/状态栏）：整个忽略
        const ptyId = tab?.panes.find((p) => p.id === paneId)?.ptyId
        const text = formatDroppedPaths(payload.paths)
        if (!ptyId || text === '') return
        void ptyWrite(ptyId, text)
      })
      if (disposed) un() // 卸载竞态：await 期间组件已卸载时立即注销
      else unlisten = un
    })
    return () => { disposed = true; unlisten?.() }
  }, [])
```

配套的组件状态（放在 App 顶部）：`const [dropPaneId, setDropPaneId] = useState<string | null>(null)`，并从 `react` import 补 `useState`。把 `dropPaneId` 传给 `TabPanes`。

顶部 import 补：
```ts
import { formatDroppedPaths, paneAtPoint, toLogicalPoint } from './fileDrop'
import { getPaneSlotRects } from './paneDropDom'
import { ptyWrite } from './ipc'
```

`src/components/TabPanes.tsx`：组件 props 加 `dropPaneId?: string | null`，改**第 290 行**（就是带 `data-pane-id={pane.id}` 的那个元素，见第 297 行）：

```tsx
// 改动前：
className={`pane${showTitlebar && focused ? ' pane-focused' : ''}`}
// 改动后：
className={`pane${showTitlebar && focused ? ' pane-focused' : ''}${pane.id === dropPaneId ? ' pane-drop-target' : ''}`}
```

`dropPaneId` 需要从 `TabPanes` 一路传到渲染该元素的子组件——按该文件现有的 props 传递方式接入，不要另起一套（例如新建 context）。

`src/App.css` 加（**颜色只用主题变量**）：

```css
/* 文件拖放落点高亮（spec §5）：只描边、不改布局——改内边距或边框宽度会让
   TerminalLayer/TerminalView 两层 ResizeObserver 重新测量并触发一次 fit()，
   拖拽过程中终端会抖动。用 inset 阴影不参与布局。 */
.pane-drop-target { box-shadow: inset 0 0 0 2px var(--color-accent); }
```

- [ ] **Step 3: 验证**

Run: `npx vitest run` → 应为 **639**，与 Task 7 结束时一致（本任务不新增单测：接线部分依赖真实 Tauri 事件与真实布局，jsdom 里 `getBoundingClientRect()` 恒返回全 0，写出来的"测试"证明不了任何事——这正是 `paneDropDom.ts` 顶部注释已经确立的判断）
Run: `npm run build`

- [ ] **Step 4: 提交**

```bash
git add src/App.tsx src/App.css src/components/TabPanes.tsx
git commit -m "feat(drop): 从 Finder 拖文件进终端写入路径

Tauri 接管了原生拖放，HTML5 drop 不触发；改走 onDragDropEvent，
拿到的是真实路径。坐标是物理像素，需按 DPR 换算再做窗格命中。"
```

- [ ] **Step 5: 真机验收（由用户执行）**

1. 从 Finder 拖一个**普通文件**到终端窗格 → 路径出现在命令行，带引号，末尾有空格
2. 拖一个**名字含空格或中文**的文件 → 引号包裹正确，没被拆成两段
3. 拖**多个文件** → 多个路径以空格分隔
4. **⌘D 分屏后拖到右边那个窗格** → 写进右边，不是写进聚焦的那个
5. 拖到**标签栏或主页** → 什么都不发生，不报错
6. **拖动过程中目标窗格有高亮边框**，移开后消失

---

### Task 9: 中文输入法探针

**这是探针，不是修复任务。** 允许的产出包括"这是 xterm 上游缺陷，绕不过去"——spec §6 已写明该退出条件。**不要**为了让任务看起来完成而拼一个把别的输入路径搞坏的 workaround。

**Files:**
- Modify: `src/components/TerminalView.tsx`（**临时**仪表，定位完成后必须删除）

**背景（spec §2.7 已定位到嫌疑机制）：** xterm 6.0 的 `CompositionHelper` 把 `compositionend` 的真正发送推迟到 `setTimeout(…, 0)`，由 `_isSendingComposition` 守卫；任何不在白名单（keyCode 20/229/16/17/18）内的 keydown 落进这个窗口，就会调用 `_finalizeComposition(false)` 把它翻成 false，**那次发送永久丢失**。下一次按键的 `_handleAnyTextareaChanges()` 再把 textarea 里攒着的内容一并冲出——与用户描述的"第一个要按两次、紧跟着的第二个一下就出来"逐字吻合。缺的是"谁在那个窗口里插了一脚"。

用户已确认：**只有终端里会，主页搜索框（普通 React `<input>`）正常** → 问题在 xterm 层，不在 WKWebView 层。

- [ ] **Step 1: 装仪表**

在 `TerminalView` 的 effect 里临时加（`term.open(el)` 之后）：

```ts
    // 【临时仪表 —— 定位中文输入法首个标点需按两次的问题，定位完成后删除】
    const ta = el.querySelector('textarea')
    const log = (tag: string, extra: Record<string, unknown>) =>
      console.log(`[ime] ${tag}`, JSON.stringify({ t: Math.round(performance.now()), ...extra }))
    ta?.addEventListener('keydown', (e) => log('keydown', { key: e.key, code: e.keyCode, composing: e.isComposing }), true)
    ta?.addEventListener('compositionstart', () => log('compositionstart', {}))
    ta?.addEventListener('compositionupdate', (e) => log('compositionupdate', { data: (e as CompositionEvent).data }))
    ta?.addEventListener('compositionend', (e) => log('compositionend', { data: (e as CompositionEvent).data }))
    ta?.addEventListener('input', (e) => log('input', { type: (e as InputEvent).inputType, data: (e as InputEvent).data, value: ta.value }))
    term.onData((d) => log('onData', { d: JSON.stringify(d) }))
```

- [ ] **Step 2: 采集**

`npm run tauri build`，请用户在终端里切到中文输入法，**打一个 `？`，再紧跟着打一个 `？`**，然后打一个 `（`。把 devtools 控制台里全部 `[ime]` 行回传。

- [ ] **Step 3: 定位**

对照日志判断：

- 第一个 `？` 之后是否出现了 `compositionend`，其后 `onData` 是否**缺失**；
- 在 `compositionend` 与本该发生的 `onData` 之间，是否插入了一个 keydown，其 `code` **不在** 20/229/16/17/18 之列（那就是取消发送的元凶）；
- 若元凶 keydown 来自 aTerm 自己（`App.tsx` 在 window 上装了捕获阶段的 keydown 监听器），则修法在我们这边；若来自 IME 本身发出的按键，则是 xterm 上游缺陷。

- [ ] **Step 4: 依判定分支**

- **元凶在 aTerm 侧** → 写一个能复现该事件序列的回归测试（可在 jsdom 里手工派发 keydown/composition 事件序列，因为此时被测对象是**我们自己的监听器**，不是 IME），再修，再验。
- **元凶在 xterm 侧** → 评估 `term.attachCustomKeyEventHandler` 能否在事件到达 `CompositionHelper` 之前拦下那个按键。可行则实现并由用户真机验收；不可行则**如实报告并从 V3.0 移出**，在 `docs/BACKLOG.md` 记一条，附本次日志证据。

- [ ] **Step 5: 删除仪表并提交**

无论走哪个分支，**Step 1 加的全部 `[ime]` 日志代码必须删除**。

```bash
git add src/components/TerminalView.tsx
git commit -m "fix(ime): <依实际结论填写>"
```

- [ ] **Step 6: 真机验收（由用户执行）**

在终端里切中文输入法，打第一个 `？` —— **按一次就出字**。再打 `（`，同样一次出字。

**不接受任何自动化测试作为本条的通过依据**（spec §6）。

---

## 收尾

全部任务完成后：

- [ ] `npx vitest run` 与 `cargo test --manifest-path src-tauri/Cargo.toml` 全绿，数量不低于 639 / 112
- [ ] `npm run tauri build` 成功
- [ ] 用 `superpowers:finishing-a-development-branch` 收尾（合并 `feature/v3-0-fixes`）
- [ ] 若 Task 9 的结论是"上游缺陷"，在 `docs/BACKLOG.md` 记一条并在收尾报告里明说**第 6 条未修复**，不要让它悄悄消失
