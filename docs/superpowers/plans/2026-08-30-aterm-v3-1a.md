# aTerm V3.1a Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让侧栏/主页/标签栏三个列表面好用起来——最近会话填满、按日期分组、单击选中双击打开、右键改名/定位文件夹/隐藏/移除，`＋` 弹出与 ⌘D 相同的会话选择器。

**Architecture:** 新增一个持久化 store（`library`）承载别名/隐藏名单/移除名单，配一组纯函数（`sessionList.ts`）做派生查询；把 `PanePicker` 的选择界面抽成共享的 `SessionPicker`，`＋` 与 ⌘D 各传自己的 `onPick`；新增一个 Rust 命令做「在访达中显示」。

**Tech Stack:** React 19 / TypeScript / zustand / vitest / Rust (Tauri 2.11)

**Spec:** `docs/superpowers/specs/2026-08-30-aterm-v3-1a-design.md`

## Global Constraints

- **本仓库没有 jest-dom。** 断言只用 `toBe()` / `toBeTruthy()` / `toBeNull()` / `toEqual()` / `classList.contains()` / `toHaveBeenCalledWith()`。**禁止** `toBeInTheDocument` / `toHaveClass` / `toHaveValue` / `toHaveTextContent`——它们不存在，会直接报错。
- **测试绝不碰真实 `~/.claude`**：Rust 一律 `tempfile::tempdir()`，前端一律 mock `../ipc`。
- **颜色一律取主题 CSS 变量**（`var(--color-*)`）。28 套主题 × 3 种外观模式，**任何硬编码 hex / rgb / hsl / 颜色名都是缺陷**。
- **每个测试必须能回答「它会因为什么而失败」。** 断言不出错误行为的测试不算测试。
- **受保护文件，本期一律不得修改**：`src/components/ConversationPanel.tsx`、`src/components/TerminalView.tsx`。
- **别名的 localStorage 键与分隔符不得更改**：键名 `aterm.overview.names`，键格式 `${dirName}::${rootKey}`（双冒号，由 `overview.ts` 的 `blockKey` 生成）。改任何一个都会让用户已改过的会话名**静默作废**。见 spec §3.1。
- 前端测试：`npx vitest run`（**`package.json` 里没有 `test` 脚本**）。单文件：`npx vitest run src/__tests__/<file>`。
- Rust 测试：`source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml`
- 类型检查 + 构建：`npm run build`
- **基线**：前端 **653** 测试 / 43 文件；Rust **112** 测试。每个任务给出的期望数 = 基线 + 累计新增；若与实测不符，**以实测为准并在报告里说明**。
- **已知的既有 flaky**：`src/__tests__/TabBar.test.tsx` 有一条拖拽安全网用例约 8% 概率在全量并行时失败（已用 A/B 实测确认与本仓库近期改动无关，main 上同样复现）。撞上重跑一次即可，**不要去修它，也不要算作自己的失败**。
- 分支：`feature/v3-1a-lists-menus`（已创建，spec 已提交在其上）。

---

### Task 1: `sessionList.ts` 三个纯函数

**Files:**
- Create: `src/sessionList.ts`
- Test: `src/__tests__/sessionList.test.ts`

**Interfaces:**
- Consumes: `blockKey` from `src/store/overview.ts`（已存在，签名 `blockKey(dirName: string, rootKey: string): string`，返回 `` `${dirName}::${rootKey}` ``）
- Produces:
  ```ts
  export function displayTitle(
    thread: { rootKey: string; title: string; titled: boolean },
    dirName: string,
    aliases: Record<string, string>,
  ): string
  export function isSessionRemoved(removedAtMs: number | undefined, lastActivityMs: number): boolean
  export function groupRecentByDate<T extends { lastActivityMs: number }>(
    items: T[], now: number,
  ): { label: '今天' | '昨天' | '更早'; items: T[] }[]
  ```

**背景（实现者必读）：** 后端 `ThreadInfo.title` 在会话还没有真实标题时是 `session_id` 的前 8 位（一串 uuid 碎片），`titled: false` 就是这个情况的标记。列表直接渲染 `title` 会让用户看到一串十六进制。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/sessionList.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { displayTitle, groupRecentByDate, isSessionRemoved } from '../sessionList'

describe('displayTitle', () => {
  it('有别名时优先用别名', () => {
    const aliases = { '-tmp-a::r1': '我的任务' }
    expect(displayTitle({ rootKey: 'r1', title: '修登录', titled: true }, '-tmp-a', aliases)).toBe('我的任务')
  })
  it('无别名时用真实标题', () => {
    expect(displayTitle({ rootKey: 'r1', title: '修登录', titled: true }, '-tmp-a', {})).toBe('修登录')
  })
  it('titled 为 false 时给「新对话」，绝不显示 uuid 前 8 位', () => {
    // 后端在没有真实标题时把 title 填成 session_id 前 8 位；直接渲染会让列表出现一串十六进制
    expect(displayTitle({ rootKey: 'r1', title: 'ebd067d4', titled: false }, '-tmp-a', {})).toBe('新对话')
  })
  it('别名优先级高于「新对话」——用户给未命名会话起了名就该显示那个名', () => {
    const aliases = { '-tmp-a::r1': '临时试验' }
    expect(displayTitle({ rootKey: 'r1', title: 'ebd067d4', titled: false }, '-tmp-a', aliases)).toBe('临时试验')
  })
  it('别名按「项目::会话」复合键取，不会跨项目误命中', () => {
    const aliases = { '-tmp-b::r1': 'B 的名字' }
    expect(displayTitle({ rootKey: 'r1', title: '修登录', titled: true }, '-tmp-a', aliases)).toBe('修登录')
  })
})

describe('isSessionRemoved', () => {
  it('从未移除过 → 不隐去', () => {
    expect(isSessionRemoved(undefined, 100)).toBe(false)
  })
  it('移除后没有新活动 → 隐去', () => {
    expect(isSessionRemoved(200, 100)).toBe(true)
  })
  it('移除后又有新活动 → 自动回归（这正是「下次再用它就出现」）', () => {
    expect(isSessionRemoved(200, 300)).toBe(false)
  })
  it('边界：活动时间正好等于移除时刻 → 仍隐去（同一时刻不算「又用了一次」）', () => {
    expect(isSessionRemoved(200, 200)).toBe(true)
  })
})

describe('groupRecentByDate', () => {
  // 2026-08-30 12:00 本地时间
  const now = new Date(2026, 7, 30, 12, 0, 0).getTime()
  const at = (d: number, h: number) => new Date(2026, 7, d, h, 0, 0).getTime()

  it('分成今天/昨天/更早三组，顺序固定', () => {
    const items = [
      { lastActivityMs: at(30, 9) },   // 今天
      { lastActivityMs: at(29, 9) },   // 昨天
      { lastActivityMs: at(20, 9) },   // 更早
    ]
    const g = groupRecentByDate(items, now)
    expect(g.map((x) => x.label)).toEqual(['今天', '昨天', '更早'])
    expect(g[0].items.length).toBe(1)
    expect(g[1].items.length).toBe(1)
    expect(g[2].items.length).toBe(1)
  })
  it('空组不产出——没有「昨天」的会话时不该出现一个空的「昨天」标题', () => {
    const g = groupRecentByDate([{ lastActivityMs: at(30, 9) }], now)
    expect(g.map((x) => x.label)).toEqual(['今天'])
  })
  it('午夜边界：今天 00:01 属于今天，昨天 23:59 属于昨天', () => {
    const g = groupRecentByDate(
      [{ lastActivityMs: at(30, 0) + 60_000 }, { lastActivityMs: at(29, 23) + 59 * 60_000 }],
      now,
    )
    expect(g.map((x) => x.label)).toEqual(['今天', '昨天'])
  })
  it('保持传入顺序，不重新排序（调用方已按活跃时间排好）', () => {
    const a = { lastActivityMs: at(30, 9) }
    const b = { lastActivityMs: at(30, 11) }
    const g = groupRecentByDate([a, b], now)
    expect(g[0].items[0]).toBe(a)
    expect(g[0].items[1]).toBe(b)
  })
  it('空输入返回空数组', () => {
    expect(groupRecentByDate([], now)).toEqual([])
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/sessionList.test.ts`
Expected: FAIL —— 模块 `../sessionList` 不存在。

- [ ] **Step 3: 实现**

新建 `src/sessionList.ts`：

```ts
// 三个列表面（侧栏「最近会话」、主页项目卡片、总览方块）共用的派生查询。
// 与 store 分开放，是为了能当纯函数单测——store 里只放状态与写操作。
//
// 命名注意：本文件叫 sessionList.ts，store 叫 store/library.ts。两者刻意不同名，
// 只差一层目录的同名文件在 import 时极易看错。
import { blockKey } from './store/overview'

/** 列表里该显示的标题。优先级：用户别名 > 真实标题 > 「新对话」。
 *  最后一档是必需的：后端在会话尚无标题时把 title 填成 session_id 前 8 位
 *  （见 src-tauri/src/sessions/scan.rs 的 title 回退），titled 就是这个情况的标记。
 *  直接渲染 title 会让列表里出现一串十六进制。 */
export function displayTitle(
  thread: { rootKey: string; title: string; titled: boolean },
  dirName: string,
  aliases: Record<string, string>,
): string {
  const alias = aliases[blockKey(dirName, thread.rootKey)]
  if (alias) return alias
  return thread.titled ? thread.title : '新对话'
}

/** 该会话此刻是否应从「最近会话」里隐去。移除之后只要又有新活动就自动回归——
 *  这实现了「下次再用它的时候默认可以出现」，不需要任何额外 UI，也不需要用户
 *  记得去哪里恢复。等号取「仍隐去」：同一毫秒不算「又用了一次」。 */
export function isSessionRemoved(removedAtMs: number | undefined, lastActivityMs: number): boolean {
  if (removedAtMs === undefined) return false
  return lastActivityMs <= removedAtMs
}

const DAY_LABELS = ['今天', '昨天', '更早'] as const

/** 按本地日历日分「今天/昨天/更早」。不排序——调用方已按活跃时间降序排好，
 *  这里只做分桶，保持传入顺序。空桶不产出，避免出现一个下面什么都没有的标题。 */
export function groupRecentByDate<T extends { lastActivityMs: number }>(
  items: T[],
  now: number,
): { label: (typeof DAY_LABELS)[number]; items: T[] }[] {
  const startOfToday = new Date(now)
  startOfToday.setHours(0, 0, 0, 0)
  const todayMs = startOfToday.getTime()
  const yesterdayMs = todayMs - 24 * 60 * 60 * 1000

  const buckets: T[][] = [[], [], []]
  for (const it of items) {
    if (it.lastActivityMs >= todayMs) buckets[0].push(it)
    else if (it.lastActivityMs >= yesterdayMs) buckets[1].push(it)
    else buckets[2].push(it)
  }
  return DAY_LABELS
    .map((label, i) => ({ label, items: buckets[i] }))
    .filter((g) => g.items.length > 0)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/sessionList.test.ts` → PASS（15 个用例）
Run: `npx vitest run` → 应为 **668**（基线 653 + 15）
Run: `npm run build` → 无类型错误

- [ ] **Step 5: 提交**

```bash
git add src/sessionList.ts src/__tests__/sessionList.test.ts
git commit -m "feat(list): 加 sessionList 三个纯函数——显示标题、移除判定、日期分组"
```

---

### Task 2: `library` store（别名搬家 + 隐藏名单 + 移除名单）

**Files:**
- Create: `src/store/library.ts`
- Modify: `src/store/overview.ts`（迁出 `names` / `rename` / `clearName`）
- Modify: `src/components/OverviewPage.tsx`（改从 `library` 读别名）
- Test: `src/__tests__/library.test.ts`
- Modify: `src/__tests__/overviewStore.test.ts`（原本测 `names`/`rename` 的用例随之迁移）

**Interfaces:**
- Consumes: `blockKey`（仍由 `src/store/overview.ts` 导出，**不要移动它**）
- Produces:
  ```ts
  export const useLibrary: /* zustand store */
  // 状态：aliases: Record<string,string>；hiddenProjects: Record<string,true>；removedSessions: Record<string,number>
  // 方法：rename(key,name) / clearAlias(key) / hideProject(dirName) / unhideProject(dirName)
  //       / removeSession(key) / restoreSession(key)
  ```

**⚠️ 本任务有真实的数据风险，请逐字遵守：**

用户已经改过的会话名存在 `localStorage` 的 **`aterm.overview.names`** 下，键格式是 **`${dirName}::${rootKey}`**（双冒号，由 `blockKey` 生成）。

- **不要**把存储键改成 `aterm.library.aliases` 之类"更整齐"的名字；
- **不要**把键格式换成 `threadKey` 的单冒号 `${dirName}:${rootKey}`。

任何一个改动都会让用户已有的名字读不出来，**而且不报错，就是空**。这是搬家，不是重建。

新增的两份数据用新键：`aterm.library.hiddenProjects`、`aterm.library.removedSessions`。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/library.test.ts`。**必须沿用 `src/__tests__/layout.test.ts` 的写法**——持久化读取发生在模块求值时，所以每个用例都要先 mock localStorage 再动态 import：

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'

function mockLocalStorage(initial: Record<string, string> = {}) {
  const store = new Map<string, string>(Object.entries(initial))
  const ls = {
    getItem: vi.fn((k: string) => (store.has(k) ? store.get(k)! : null)),
    setItem: vi.fn((k: string, v: string) => { store.set(k, v) }),
    removeItem: vi.fn((k: string) => { store.delete(k) }),
    clear: vi.fn(() => store.clear()),
  }
  vi.stubGlobal('localStorage', ls)
  return ls
}

beforeEach(() => { vi.resetModules() })
afterEach(() => { vi.unstubAllGlobals() })

describe('library store — 别名搬家（最重要的一组）', () => {
  it('读得到总览页时代已经存下的名字（键名与键格式都不能变）', async () => {
    // 这条用例就是为了钉死 spec §3.1：存储键必须仍是 aterm.overview.names，
    // 键格式必须仍是 `${dirName}::${rootKey}`。任何一个改了，这里就会读到 undefined。
    mockLocalStorage({ 'aterm.overview.names': JSON.stringify({ '-tmp-a::r1': '旧名字' }) })
    const { useLibrary } = await import('../store/library')
    expect(useLibrary.getState().aliases['-tmp-a::r1']).toBe('旧名字')
  })

  it('写入仍然落在 aterm.overview.names 这个键上', async () => {
    const ls = mockLocalStorage()
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().rename('-tmp-a::r1', '新名字')
    expect(ls.setItem).toHaveBeenCalledWith('aterm.overview.names', JSON.stringify({ '-tmp-a::r1': '新名字' }))
  })

  it('坏数据不炸：值不是对象时退回空表', async () => {
    mockLocalStorage({ 'aterm.overview.names': '"不是对象"' })
    const { useLibrary } = await import('../store/library')
    expect(useLibrary.getState().aliases).toEqual({})
  })
})

describe('library store — rename / clearAlias', () => {
  it('空白名字视为清除，不落盘空标题', async () => {
    mockLocalStorage({ 'aterm.overview.names': JSON.stringify({ '-tmp-a::r1': '旧' }) })
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().rename('-tmp-a::r1', '   ')
    expect(useLibrary.getState().aliases['-tmp-a::r1']).toBe(undefined)
  })
  it('两侧空白被 trim 掉，不会带着空格落盘', async () => {
    mockLocalStorage()
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().rename('-tmp-a::r1', '  我的任务  ')
    expect(useLibrary.getState().aliases['-tmp-a::r1']).toBe('我的任务')
  })
  it('clearAlias 删除该键', async () => {
    mockLocalStorage({ 'aterm.overview.names': JSON.stringify({ '-tmp-a::r1': '旧' }) })
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().clearAlias('-tmp-a::r1')
    expect(useLibrary.getState().aliases['-tmp-a::r1']).toBe(undefined)
  })
})

describe('library store — 隐藏项目', () => {
  it('hide 之后 hiddenProjects 里有它，并持久化到新键', async () => {
    const ls = mockLocalStorage()
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().hideProject('-tmp-a')
    expect(useLibrary.getState().hiddenProjects['-tmp-a']).toBe(true)
    expect(ls.setItem).toHaveBeenCalledWith('aterm.library.hiddenProjects', JSON.stringify({ '-tmp-a': true }))
  })
  it('unhide 往返：隐藏再取消，回到空表', async () => {
    mockLocalStorage()
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().hideProject('-tmp-a')
    useLibrary.getState().unhideProject('-tmp-a')
    expect(useLibrary.getState().hiddenProjects).toEqual({})
  })
  it('读得到已持久化的隐藏名单', async () => {
    mockLocalStorage({ 'aterm.library.hiddenProjects': JSON.stringify({ '-tmp-a': true }) })
    const { useLibrary } = await import('../store/library')
    expect(useLibrary.getState().hiddenProjects['-tmp-a']).toBe(true)
  })
})

describe('library store — 移除会话', () => {
  it('removeSession 记下当前时刻', async () => {
    mockLocalStorage()
    vi.setSystemTime(new Date(1_700_000_000_000))
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().removeSession('-tmp-a::r1')
    expect(useLibrary.getState().removedSessions['-tmp-a::r1']).toBe(1_700_000_000_000)
    vi.useRealTimers()
  })
  it('restoreSession 删除该记录', async () => {
    mockLocalStorage({ 'aterm.library.removedSessions': JSON.stringify({ '-tmp-a::r1': 123 }) })
    const { useLibrary } = await import('../store/library')
    useLibrary.getState().restoreSession('-tmp-a::r1')
    expect(useLibrary.getState().removedSessions['-tmp-a::r1']).toBe(undefined)
  })
})
```

**注意**：用到 `vi.setSystemTime` 的那条要在文件顶部的 `beforeEach` 里加 `vi.useFakeTimers()`，或在该用例内自行 `vi.useFakeTimers()` 后再 `vi.useRealTimers()`。按你实际写法调整，但**必须保证不影响其它用例**。

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/library.test.ts`
Expected: FAIL —— 模块 `../store/library` 不存在。

- [ ] **Step 3: 实现**

新建 `src/store/library.ts`。`readJson` / `persist` 两个辅助函数与 `overview.ts` 里的**完全相同**——把它们从 `overview.ts` 导出复用，不要复制第二份：

```ts
// 三个列表面（侧栏、主页、总览）共用的用户数据：会话别名、主页隐藏的项目、
// 从「最近会话」移除的会话。持久化写法沿用 store/overview.ts（模块加载时读一次，
// 写入集中在 persist 里，不引入持久化库）。
//
// 别名原先住在 store/overview.ts 里（只服务总览方块），现在提升为三处共用。
// 【存储键与键格式一个字都不能改】：用户已经改过的名字就存在 aterm.overview.names
// 下、键是 `${dirName}::${rootKey}`。换成更整齐的新键名、或换成 threadKey 的单冒号
// 格式，都会让这些名字静默作废（读出来是空，不报任何错）。这是搬家，不是重建。
import { create } from 'zustand'
import { persist, readJson } from './overview'

const ALIASES_KEY = 'aterm.overview.names'          // 历史键名，见上方注释，不要改
const HIDDEN_KEY = 'aterm.library.hiddenProjects'
const REMOVED_KEY = 'aterm.library.removedSessions'

type LibraryState = {
  aliases: Record<string, string>
  hiddenProjects: Record<string, true>
  removedSessions: Record<string, number>
  rename(key: string, name: string): void
  clearAlias(key: string): void
  hideProject(dirName: string): void
  unhideProject(dirName: string): void
  removeSession(key: string): void
  restoreSession(key: string): void
}

export const useLibrary = create<LibraryState>((set, get) => ({
  aliases: readJson<Record<string, string>>(ALIASES_KEY, {}),
  hiddenProjects: readJson<Record<string, true>>(HIDDEN_KEY, {}),
  removedSessions: readJson<Record<string, number>>(REMOVED_KEY, {}),

  // 一律先 trim 再判空——空白规则因此只有一条，而不是"全空白清除、两侧填充却原样
  // 落盘"。带着空格落盘后每次渲染都带着那对空格，用户看不出多的是什么、也删不掉。
  rename: (key, name) => {
    const trimmed = name.trim()
    if (trimmed === '') { get().clearAlias(key); return }
    const next = { ...get().aliases, [key]: trimmed }
    persist(ALIASES_KEY, next)
    set({ aliases: next })
  },
  clearAlias: (key) => {
    const next = { ...get().aliases }
    delete next[key]
    persist(ALIASES_KEY, next)
    set({ aliases: next })
  },
  hideProject: (dirName) => {
    const next = { ...get().hiddenProjects, [dirName]: true as const }
    persist(HIDDEN_KEY, next)
    set({ hiddenProjects: next })
  },
  unhideProject: (dirName) => {
    const next = { ...get().hiddenProjects }
    delete next[dirName]
    persist(HIDDEN_KEY, next)
    set({ hiddenProjects: next })
  },
  removeSession: (key) => {
    const next = { ...get().removedSessions, [key]: Date.now() }
    persist(REMOVED_KEY, next)
    set({ removedSessions: next })
  },
  restoreSession: (key) => {
    const next = { ...get().removedSessions }
    delete next[key]
    persist(REMOVED_KEY, next)
    set({ removedSessions: next })
  },
}))
```

`src/store/overview.ts` 的改动：

1. 把 `readJson` 与 `persist` 两个函数加上 `export`（供 `library.ts` 复用，不要在新文件里复制一份）；
2. 删除 `names` 字段、`rename` 方法、`clearName` 方法，以及 `NAMES_KEY` 常量和 `initialNames`；
3. **保留** `blockKey` 的导出（`sessionList.ts` 与 `library.ts` 都依赖它）；
4. 在文件顶部注释里把"自定义命名"那句改成指向 `store/library.ts`。

`src/components/OverviewPage.tsx`：把读 `useOverviewStore(...names)` / 调 `rename` / `clearName` 的地方改成 `useLibrary`。**先 grep 找出全部调用点**：

```bash
grep -n "names\|rename\|clearName" src/components/OverviewPage.tsx
```

`src/__tests__/overviewStore.test.ts`：原本测 `names` / `rename` / `clearName` 的用例迁到 `library.test.ts`（上面已经覆盖了同样的行为），从 overview 的测试里删除。**净变化应为 0**——迁走几条就在新文件里覆盖几条。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/library.test.ts` → PASS
Run: `npx vitest run` → 全绿。**期望数取决于你从 overviewStore.test.ts 迁走了几条**，请在报告里写清楚：迁走 N 条、新增 M 条、最终总数。
Run: `npm run build` → 无类型错误

- [ ] **Step 5: 提交**

```bash
git add src/store/library.ts src/store/overview.ts src/components/OverviewPage.tsx src/__tests__/
git commit -m "feat(library): 新增 library store，别名从 overview 搬家并加隐藏/移除名单

别名的 localStorage 键与 `${dirName}::${rootKey}` 键格式一字未动——
改任何一个都会让用户已改过的名字静默作废。"
```

---

### Task 3: `reveal_in_finder` Rust 命令

**Files:**
- Create: `src-tauri/src/reveal.rs`
- Modify: `src-tauri/src/lib.rs`（`mod` 声明 + `invoke_handler` 注册）
- Modify: `src/ipc.ts`（前端绑定）

**Interfaces:**
- Produces（Rust）：`#[tauri::command] pub fn reveal_in_finder(path: String) -> Result<(), String>`
- Produces（TS）：`export const revealInFinder = (path: string) => invoke<void>('reveal_in_finder', { path })`

**背景：** 仓库现有的 Rust 命令都是 PTY、会话读取、hooks 安装——**没有任何"启动外部程序"的能力**，这是新增的一类，所以校验要写实。

- [ ] **Step 1: 写失败的测试**

新建 `src-tauri/src/reveal.rs`，先只写测试模块（实现留空会编译不过，所以这一步把函数签名写出来、函数体 `todo!()`）：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn rejects_missing_path_without_spawning() {
        let tmp = tempfile::tempdir().unwrap();
        let missing = tmp.path().join("不存在的目录");
        let err = reveal_in_finder(missing.to_string_lossy().to_string()).unwrap_err();
        assert!(err.contains("不存在"), "错误文案应告诉用户路径不存在，实际：{err}");
    }

    #[test]
    fn rejects_file_because_this_command_opens_folders() {
        // 收窄到目录不只是"更严格"：两个调用点传的都是项目 cwd，语义就是打开文件夹。
        // 允许文件等于凭空多出一条"用它打开任意文件"的路径。
        let tmp = tempfile::tempdir().unwrap();
        let file = tmp.path().join("a.txt");
        std::fs::write(&file, b"x").unwrap();
        let err = reveal_in_finder(file.to_string_lossy().to_string()).unwrap_err();
        assert!(err.contains("不是文件夹"), "错误文案应说明这不是文件夹，实际：{err}");
    }

    #[test]
    fn accepts_an_existing_directory() {
        let tmp = tempfile::tempdir().unwrap();
        // 真的会拉起访达；在 CI/无 GUI 环境下 spawn 仍会成功返回（open 自己去处理）。
        // 这条只断言"校验放行了"，不断言访达真的开了——那不是单测能验的事。
        assert!(reveal_in_finder(tmp.path().to_string_lossy().to_string()).is_ok());
    }
}
```

- [ ] **Step 2: 运行测试确认它失败**

先在 `src-tauri/src/lib.rs` 顶部加 `mod reveal;`（否则新文件根本不参与编译）。

Run: `source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml`
Expected: FAIL —— `todo!()` panic 或函数未定义。

- [ ] **Step 3: 实现**

`src-tauri/src/reveal.rs` 的实现部分：

```rust
use std::path::Path;

/// 在访达里打开一个文件夹。侧栏与主页的右键菜单各有一个调用点，传的都是项目 cwd。
///
/// 这是本仓库第一个会启动外部程序的命令，所以校验写实：
/// - 只接受**已存在的目录**。不存在或不是目录一律拒绝，且在拒绝时**不调用 open**。
/// - 路径作为独立参数交给 `open`，**不经 shell、不做任何字符串拼接**，因此路径里的
///   空格、引号、`;`、`$` 等一律是字面量，没有注入面。
#[tauri::command]
pub fn reveal_in_finder(path: String) -> Result<(), String> {
    let p = Path::new(&path);
    if !p.exists() {
        return Err(format!("路径不存在：{path}"));
    }
    if !p.is_dir() {
        return Err(format!("不是文件夹：{path}"));
    }
    std::process::Command::new("open")
        .arg(p)
        .spawn()
        .map_err(|e| format!("打开访达失败：{e}"))?;
    Ok(())
}
```

`src-tauri/src/lib.rs`：确认顶部有 `mod reveal;`，并在 `invoke_handler` 的列表里加一行 `reveal::reveal_in_finder,`（放在 `confirm_exit` 之前）。

`src/ipc.ts` 末尾加：

```ts
/** 在访达中打开一个文件夹。后端只接受已存在的目录，失败时 reject 的是可直接展示给
 *  用户的中文错误字符串（与 installHooks 等命令同一约定）。 */
export const revealInFinder = (path: string) => invoke<void>('reveal_in_finder', { path })
```

- [ ] **Step 4: 运行测试确认通过**

Run: `source ~/.cargo/env && cargo test --manifest-path src-tauri/Cargo.toml` → 应为 **115**（基线 112 + 3）
Run: `npm run build` → 无类型错误

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/reveal.rs src-tauri/src/lib.rs src/ipc.ts
git commit -m "feat(reveal): 新增 reveal_in_finder 命令——只收已存在的目录，路径不经 shell"
```

---

### Task 4: 抽出共享的 `SessionPicker`

**Files:**
- Create: `src/components/SessionPicker.tsx`
- Modify: `src/components/PanePicker.tsx`
- Test: 既有的 `src/__tests__/PanePicker.test.tsx` **必须原样通过**（这是本次重构的安全网）

**Interfaces:**
- Produces:
  ```ts
  export type SessionPick =
    | { kind: 'shell' }
    | { kind: 'newConversation'; project: ProjectInfo }
    | { kind: 'resume'; project: ProjectInfo; thread: ThreadInfo }

  export function SessionPicker({ defaultProject, onPick }: {
    defaultProject?: ProjectInfo
    onPick: (pick: SessionPick) => void
  }): JSX.Element
  ```

**这是一次纯重构：行为一个字节都不该变。**

**为什么需要 `defaultProject` 这个 prop（实现者必读）：** `PanePicker` 现在会推断「新对话」的默认项目——它靠 `tab.panes` 找出"拆分出本窗格的那个窗格"，若它带 `dirName` 就直接用那个项目，免去再选一次。这个推断**绑在窗格上**，`＋` 按钮那个入口根本没有"来源窗格"可言。所以推断留在 `PanePicker` 里，把结果作为一个可选 prop 传进来；`SessionPicker` 自己不认识 `Tab`/`Pane`。

- [ ] **Step 1: 先跑一遍既有测试，记录基线**

Run: `npx vitest run src/__tests__/PanePicker.test.tsx`
把通过的用例数记进报告——重构结束时必须一条不少、一条不改。

- [ ] **Step 2: 建 `SessionPicker.tsx`**

把 `PanePicker.tsx` 里从 `const [query, setQuery]` 到 `return (...)` 结束的**选择界面部分原样搬过来**，只做这些改动：

- 组件签名换成上面 Interfaces 里的那个；
- `defaultProject` 从 prop 拿，不再从 `tab.panes` 推断；
- 三个动作（`startZsh` / `startNewConversationIn` / `startResume`）的函数体换成调用 `onPick(...)`：
  ```ts
  const pickShell = () => onPick({ kind: 'shell' })
  const pickNewConversationIn = (p: ProjectInfo) => { setPickingProject(false); onPick({ kind: 'newConversation', project: p }) }
  const pickNewConversation = () => { if (defaultProject) pickNewConversationIn(defaultProject); else setPickingProject(true) }
  const pickResume = (p: ProjectInfo, t: ThreadInfo) => onPick({ kind: 'resume', project: p, thread: t })
  ```
- `RECENT_LIMIT`、搜索过滤、`pickingProject` 状态、全部 JSX 与 className **原样保留**（className 一改，既有测试和 CSS 都会跟着断）。

- [ ] **Step 3: `PanePicker.tsx` 改为使用它**

`PanePicker` 保留：`tab`/`paneId` 两个 prop、`defaultProject` 的推断逻辑（`ownIndex` / `sourcePane` 那段）、以及三个动作的**终点**（`startPaneTerminal`）：

```tsx
export function PanePicker({ tab, paneId }: { tab: Tab; paneId: string }) {
  const { projects } = useSessions()
  const tabId = tab.id
  const ownIndex = tab.panes.findIndex((p) => p.id === paneId)
  const sourcePane = ownIndex > 0 ? tab.panes[ownIndex - 1] : undefined
  const defaultProject = sourcePane?.dirName ? projects.find((p) => p.dirName === sourcePane.dirName) : undefined

  const onPick = (pick: SessionPick) => {
    const start = useTabs.getState().startPaneTerminal
    if (pick.kind === 'shell') { void start(tabId, paneId, { title: 'zsh' }); return }
    if (pick.kind === 'newConversation') { void start(tabId, paneId, newConversationSpec(pick.project.cwd)); return }
    const { project: p, thread: t } = pick
    void start(tabId, paneId, {
      title: t.title,
      cwd: p.cwd,
      inject: `claude --resume ${t.resumeSessionId}`,
      threadKey: `${p.dirName}:${t.rootKey}`,
      dirName: p.dirName,
      rootKey: t.rootKey,
      sessionId: t.resumeSessionId,
    })
  }

  return <SessionPicker defaultProject={defaultProject} onPick={onPick} />
}
```

**注意最后那个 `sessionId: t.resumeSessionId`** —— 这是新加的（spec §8 的 C 项）。`PanePicker` 此前自己内联拼 resume 参数、没传 sessionId，导致这条路径起的窗格无法被 `reconcilePanes` 对账。Task 9 会在 `actions.ts` 的 `resumeThread` 里做同样的事。

- [ ] **Step 4: 验证重构没有改变行为**

Run: `npx vitest run src/__tests__/PanePicker.test.tsx` → 与 Step 1 记录的用例数**完全一致**，且全部通过。
**如果有用例变红**：说明你改动了行为或 className，回去对齐，**不要改测试**。
Run: `npx vitest run` → 全绿，总数与 Task 2 结束时一致（本任务不新增测试）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/components/SessionPicker.tsx src/components/PanePicker.tsx
git commit -m "refactor(picker): 抽出共享的 SessionPicker，PanePicker 只保留窗格侧的推断与终点

顺带补上 resume 路径缺失的 sessionId（V3.0 终审留账 C 的一半）。"
```

---

### Task 5: `＋` 按钮弹出 `SessionPicker`

**Files:**
- Modify: `src/components/TabBar.tsx`
- Modify: `src/App.css`
- Test: `src/__tests__/TabBarPlus.test.tsx`（新建）

**Interfaces:**
- Consumes: `SessionPicker` / `SessionPick`（Task 4）、`newConversation` / `resumeThread` / `newTerminal`（`src/actions.ts`）

**⌘T 保持原样，仍然直接新建空终端，不弹浮层。** 分工：⌘T 是「我就要一个 shell」的快捷路径，`＋` 是「我要挑一下」的浏览路径。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/TabBarPlus.test.tsx`：

```tsx
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen, waitFor } from '@testing-library/react'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(async () => 'pty-1'),
  ptyIsAlive: vi.fn(async () => false),
  ptyKill: vi.fn(async () => {}),
  listProjects: vi.fn(async () => []),
  revealInFinder: vi.fn(async () => {}),
}))
vi.mock('../ptyBuffer', () => ({ ptyEventsReady: Promise.resolve(), attachPty: vi.fn() }))

import * as ipc from '../ipc'
import { TabBar } from '../components/TabBar'
import { useSessions } from '../store/sessions'
import { useTabs } from '../store/tabs'
import { makeThread } from './factories'

beforeEach(() => {
  useTabs.setState({ tabs: [{ id: 'home', kind: 'home', title: '主页', panes: [] }], activeId: 'home' })
  useSessions.setState({
    projects: [{
      dirName: '-tmp-a', cwd: '/tmp/a', lastActivityMs: 1,
      threads: [makeThread({ rootKey: 'r1', title: '修登录', resumeSessionId: 's1' })],
    }],
    loading: false,
  })
  vi.clearAllMocks()
})

describe('标签栏的 ＋', () => {
  it('点一下弹出选择器，而不是直接开一个空终端', () => {
    render(<TabBar />)
    fireEvent.click(screen.getByTitle(/新建/))
    // 选择器出现（「新终端（zsh）」是它固定的第一项）
    expect(screen.queryByText('新终端（zsh）')).toBeTruthy()
    // 关键：这一下不该已经起了 PTY
    expect(ipc.ptySpawn).not.toHaveBeenCalled()
  })

  it('在选择器里选一条会话 → 新建一个标签（而不是填充某个窗格）', async () => {
    render(<TabBar />)
    fireEvent.click(screen.getByTitle(/新建/))
    fireEvent.click(screen.getByText('修登录'))
    await waitFor(() => expect(ipc.ptySpawn).toHaveBeenCalled())
    const tabs = useTabs.getState().tabs
    // 多出了一个 term 标签
    expect(tabs.filter((t) => t.kind === 'term').length).toBe(1)
    expect(ipc.ptySpawn).toHaveBeenCalledWith(expect.objectContaining({ inject: 'claude --resume s1' }))
  })

  it('选「新终端（zsh）」→ 新建一个空终端标签', async () => {
    render(<TabBar />)
    fireEvent.click(screen.getByTitle(/新建/))
    fireEvent.click(screen.getByText('新终端（zsh）'))
    await waitFor(() => expect(ipc.ptySpawn).toHaveBeenCalled())
    expect(useTabs.getState().tabs.filter((t) => t.kind === 'term').length).toBe(1)
  })

  it('选完之后浮层关闭', async () => {
    render(<TabBar />)
    fireEvent.click(screen.getByTitle(/新建/))
    fireEvent.click(screen.getByText('新终端（zsh）'))
    await waitFor(() => expect(screen.queryByText('新终端（zsh）')).toBeNull())
  })
})
```

**关于独立渲染 `<TabBar />`**：可以这么做。已核对 `TabBar.tsx` 的全部 import——它只依赖
`actions` / `paneDrop*` / `paneLayout` 与 `useDnd`/`useDragGhost`/`useHint`/`useLayout`/`useTabs`
这几个纯 zustand store，**不碰 `store/status`、`store/hooksInstall`、`store/theme`**，所以上面
两个 mock 就够了。（对比：`Sidebar`/`HomePage` 会渲染 `HooksControl`，需要更多 mock，见 Task 6。）

由于本文件不渲染 `<App/>`，也就没有"App 挂载时 `refresh()` 会用空数组覆盖 setState 种下的
projects"那个陷阱（见 `src/__tests__/Sidebar.test.tsx` 顶部注释），`useSessions.setState` 可以放心用。

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/TabBarPlus.test.tsx`
Expected: FAIL —— 点 ＋ 直接起了 PTY，选择器不出现。

- [ ] **Step 3: 实现**

`TabBar.tsx`：加一个浮层状态，把 `＋` 的 `onClick` 从 `newTerminal()` 换成开浮层。浮层的关闭与定位**复用 `ContextMenu.tsx` 的做法**（portal + 点外部/Esc/失焦关闭），不新写第三套：

```tsx
const [plusMenu, setPlusMenu] = useState<{ x: number; y: number } | null>(null)

const onPlusClick = (e: ReactMouseEvent<HTMLButtonElement>) => {
  const r = e.currentTarget.getBoundingClientRect()
  setPlusMenu({ x: r.left, y: r.bottom + 4 })
}

const onPlusPick = (pick: SessionPick) => {
  setPlusMenu(null)
  if (pick.kind === 'shell') { void newTerminal(); return }
  if (pick.kind === 'newConversation') { void newConversation(pick.project.cwd); return }
  void resumeThread(pick.project.dirName, pick.project.cwd, pick.thread)
}
```

`＋` 按钮改为 `onClick={onPlusClick}`，`title` 改成 `"新建标签"`（不要写 `(⌘T)`——⌘T 现在与它行为不同了，写在一起会误导）。

浮层渲染放在 `ContextMenu` 那个已有的渲染位置旁边：

```tsx
{plusMenu && (
  <PlusMenu x={plusMenu.x} y={plusMenu.y} onPick={onPlusPick} onDismiss={() => setPlusMenu(null)} />
)}
```

`PlusMenu` 是本文件内的一个小组件：用 `createPortal` 挂到 `document.body`，外层 div 定位到 `(x, y)`，内部渲染 `<SessionPicker onPick={onPick} />`（**不传 `defaultProject`**——`＋` 没有来源窗格，「新对话」应当列出全部项目供选），并复用与 `ContextMenu` 相同的三种关闭方式（点外部 / Esc / 窗口失焦）。

`App.css` 加浮层容器样式（**颜色只用主题变量**）：

```css
/* 标签栏 ＋ 的浮层：与 .context-menu 同一层视觉语言（portal 到 body，绝对定位），
   但内容是会话选择器而非菜单项列表，所以宽高另给一档。 */
.plus-menu { position: fixed; z-index: 40; width: 320px; max-height: 60vh; overflow-y: auto;
  background: var(--color-panel); border: 1px solid var(--color-border); border-radius: 10px; padding: 6px; }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/TabBarPlus.test.tsx` → PASS（4 个用例）
Run: `npx vitest run` → 全绿（比 Task 4 结束时多 4）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/components/TabBar.tsx src/App.css src/__tests__/TabBarPlus.test.tsx
git commit -m "feat(tabbar): ＋ 改为弹出会话选择器，与 ⌘D 共用同一套界面

⌘T 保持原样直接开空终端——两者分工不同，不合并。"
```

---

### Task 6: 侧栏——去掉条数上限、按日期分组、单击选中/双击打开

**Files:**
- Modify: `src/components/Sidebar.tsx`
- Modify: `src/App.css`
- Test: `src/__tests__/SidebarList.test.tsx`（新建）

**Interfaces:**
- Consumes: `groupRecentByDate` / `displayTitle` / `isSessionRemoved`（Task 1）、`useLibrary`（Task 2）

**第 4 条不需要动布局**：`App.css:53` 的 `.sidebar-list` 已经是 `flex: 1; min-height: 0; overflow-y: auto`，本来就填满高度并滚动。"下方空间没利用"完全是 `Sidebar.tsx:37` 的 `.slice(0, 12)` 造成的——**删掉它即可，不要碰这条 CSS**。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/SidebarList.test.tsx`：

```tsx
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(async () => 'pty-1'),
  ptyIsAlive: vi.fn(async () => false),
  ptyKill: vi.fn(async () => {}),
  listProjects: vi.fn(async () => []),
  revealInFinder: vi.fn(async () => {}),
}))
vi.mock('../ptyBuffer', () => ({ ptyEventsReady: Promise.resolve(), attachPty: vi.fn() }))
vi.mock('../store/status', () => ({
  statusEventsReady: Promise.resolve(),
  useThreadStatus: () => undefined,
  useProjectStatus: () => 'unknown' as const,
  useStatusStore: (selector: (s: { statuses: Map<string, unknown> }) => unknown) => selector({ statuses: new Map() }),
  threadStatusKey: (dirName: string, rootKey: string) => `${dirName}::${rootKey}`,
}))
// 这个 mock 的形状必须与 App.test.tsx 里那份逐字一致——Sidebar 会渲染 <HooksControl/>，
// 它同时 import 了 hooksPhase 与 useHooksInstall，而 store 模块在加载时还会发起一次
// hooksStatus() 的 IPC（`hooksInstallReady`）。三个导出缺任何一个都会在 import 期就抛错，
// 表现为"整个测试文件跑不起来"，而不是某条断言失败。
vi.mock('../store/hooksInstall', () => ({
  hooksInstallReady: Promise.resolve(),
  hooksPhase: () => null,
  useHooksInstall: Object.assign(() => null, {
    getState: () => ({ dismiss: () => {}, install: async () => {}, uninstall: async () => {} }),
  }),
}))

import * as ipc from '../ipc'
import { Sidebar } from '../components/Sidebar'
import { useSessions } from '../store/sessions'
import { useTabs } from '../store/tabs'
import { makeThread } from './factories'

const dayMs = 24 * 60 * 60 * 1000

function seed(n: number) {
  useSessions.setState({
    projects: [{
      dirName: '-tmp-a', cwd: '/tmp/a', lastActivityMs: Date.now(),
      threads: Array.from({ length: n }, (_, i) =>
        makeThread({ rootKey: `r${i}`, title: `会话${i}`, lastActivityMs: Date.now() - i * 1000 })),
    }],
    loading: false,
  })
}

beforeEach(() => {
  useTabs.setState({ tabs: [{ id: 'home', kind: 'home', title: '主页', panes: [] }], activeId: 'home' })
  vi.clearAllMocks()
})

describe('侧栏最近会话', () => {
  it('超过 12 条也全部渲染——列表本就填满高度并滚动，不该再截断', () => {
    seed(20)
    render(<Sidebar />)
    expect(screen.queryByText('会话19')).toBeTruthy()
  })

  it('按今天/昨天/更早分组，且不产出空组', () => {
    const now = Date.now()
    useSessions.setState({
      projects: [{
        dirName: '-tmp-a', cwd: '/tmp/a', lastActivityMs: now,
        threads: [
          makeThread({ rootKey: 'r1', title: '今天的', lastActivityMs: now }),
          makeThread({ rootKey: 'r2', title: '很早的', lastActivityMs: now - 10 * dayMs }),
        ],
      }],
      loading: false,
    })
    render(<Sidebar />)
    expect(screen.queryByText('今天')).toBeTruthy()
    expect(screen.queryByText('更早')).toBeTruthy()
    expect(screen.queryByText('昨天')).toBeNull()   // 没有昨天的会话就不该有这个标题
  })

  it('单击只选中不打开——这正是防误触的要点', () => {
    seed(3)
    render(<Sidebar />)
    fireEvent.click(screen.getByText('会话0'))
    expect(ipc.ptySpawn).not.toHaveBeenCalled()
    expect(useTabs.getState().tabs.filter((t) => t.kind === 'term').length).toBe(0)
  })

  it('单击后该行带上选中态 class', () => {
    seed(3)
    render(<Sidebar />)
    const row = screen.getByText('会话0').closest('.side-item')!
    expect(row.classList.contains('side-item-selected')).toBe(false)
    fireEvent.click(screen.getByText('会话0'))
    expect(row.classList.contains('side-item-selected')).toBe(true)
  })

  it('双击才打开', async () => {
    seed(3)
    render(<Sidebar />)
    fireEvent.doubleClick(screen.getByText('会话0'))
    expect(ipc.ptySpawn).toHaveBeenCalled()
  })

  it('未命名会话显示「新对话」而不是 uuid 前 8 位', () => {
    useSessions.setState({
      projects: [{
        dirName: '-tmp-a', cwd: '/tmp/a', lastActivityMs: Date.now(),
        threads: [makeThread({ rootKey: 'r1', title: 'ebd067d4', titled: false, lastActivityMs: Date.now() })],
      }],
      loading: false,
    })
    render(<Sidebar />)
    expect(screen.queryByText('新对话')).toBeTruthy()
    expect(screen.queryByText('ebd067d4')).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/SidebarList.test.tsx`
Expected: FAIL —— 只渲染 12 条、无分组标题、单击就打开。

- [ ] **Step 3: 实现**

`Sidebar.tsx`：

1. 删掉 `.slice(0, 12)`；
2. 过滤掉已移除的会话：用 `useLibrary` 的 `removedSessions` 配 `isSessionRemoved(removedSessions[blockKey(p.dirName, t.rootKey)], t.lastActivityMs)`；
3. 用 `groupRecentByDate(recent, Date.now())` 分组，每组渲染一个 `<div className="section-label">{label}</div>` 再渲染组内条目（原来那个固定的「最近会话」标题去掉——分组标题取代了它）；
4. 选中态：`const [selected, setSelected] = useState<string | null>(null)`，存 `blockKey`；
5. 单击 → `setSelected(key)`；双击 → `resumeThread(...)`。既有的 `onItemClick` 里那段"拖拽后抑制 click"的逻辑（`suppressClickRef`）**保持不动**，双击处理器同样要尊重它；
6. 标题渲染改走 `displayTitle(t, p.dirName, aliases)`。

`App.css` 加选中态（**颜色只用主题变量**）：

```css
.side-item-selected { background: var(--color-elevated); }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/SidebarList.test.tsx` → PASS（6 个用例）
Run: `npx vitest run` → 全绿（比 Task 5 结束时多 6）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/components/Sidebar.tsx src/App.css src/__tests__/SidebarList.test.tsx
git commit -m "feat(sidebar): 去掉 12 条上限、按日期分组、单击选中双击打开"
```

---

### Task 7: 侧栏右键菜单（重命名 / 在访达中显示 / 从列表移除）

**Files:**
- Modify: `src/components/Sidebar.tsx`
- Modify: `src/App.css`
- Test: `src/__tests__/SidebarMenu.test.tsx`（新建）

**Interfaces:**
- Consumes: `ContextMenu`（`src/components/ContextMenu.tsx`，签名 `{ x: number; y: number; items: { label: string; onSelect: () => void }[]; onDismiss: () => void }`）、`useLibrary`（Task 2）、`revealInFinder`（Task 3）

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/SidebarMenu.test.tsx`（mock 块与 Task 6 那个文件相同，照抄）：

```tsx
describe('侧栏右键菜单', () => {
  it('右键弹出三项', () => {
    seed(2)
    render(<Sidebar />)
    fireEvent.contextMenu(screen.getByText('会话0'))
    expect(screen.queryByText('重命名')).toBeTruthy()
    expect(screen.queryByText('在访达中显示')).toBeTruthy()
    expect(screen.queryByText('从列表移除')).toBeTruthy()
  })

  it('「在访达中显示」用项目 cwd 调后端', () => {
    seed(2)
    render(<Sidebar />)
    fireEvent.contextMenu(screen.getByText('会话0'))
    fireEvent.click(screen.getByText('在访达中显示'))
    expect(ipc.revealInFinder).toHaveBeenCalledWith('/tmp/a')
  })

  it('「从列表移除」后该条从列表消失', async () => {
    seed(2)
    render(<Sidebar />)
    fireEvent.contextMenu(screen.getByText('会话0'))
    fireEvent.click(screen.getByText('从列表移除'))
    await waitFor(() => expect(screen.queryByText('会话0')).toBeNull())
    expect(screen.queryByText('会话1')).toBeTruthy()   // 只移除被点的那一条
  })

  it('重命名：回车提交后显示新名字', async () => {
    seed(2)
    render(<Sidebar />)
    fireEvent.contextMenu(screen.getByText('会话0'))
    fireEvent.click(screen.getByText('重命名'))
    const input = screen.getByDisplayValue('会话0') as HTMLInputElement
    fireEvent.change(input, { target: { value: '我的任务' } })
    fireEvent.keyDown(input, { key: 'Enter' })
    await waitFor(() => expect(screen.queryByText('我的任务')).toBeTruthy())
  })

  it('重命名：Esc 取消，名字不变', async () => {
    seed(2)
    render(<Sidebar />)
    fireEvent.contextMenu(screen.getByText('会话0'))
    fireEvent.click(screen.getByText('重命名'))
    const input = screen.getByDisplayValue('会话0') as HTMLInputElement
    fireEvent.change(input, { target: { value: '不该生效' } })
    fireEvent.keyDown(input, { key: 'Escape' })
    await waitFor(() => expect(screen.queryByText('会话0')).toBeTruthy())
    expect(screen.queryByText('不该生效')).toBeNull()
  })
})
```

**每个用例开头都要 `useLibrary.setState({ aliases: {}, hiddenProjects: {}, removedSessions: {} })`**，否则前一个用例写进 store 的别名/移除记录会漏到下一个用例——那类污染表现为"单跑绿、全量红"，极难查。请在该文件的 `beforeEach` 里加上。

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/SidebarMenu.test.tsx`
Expected: FAIL —— 右键没有菜单。

- [ ] **Step 3: 实现**

`Sidebar.tsx`：

1. 加 `const [menu, setMenu] = useState<{ x: number; y: number; p: ProjectInfo; t: ThreadInfo } | null>(null)`；
2. `SidebarItem` 加 `onContextMenu` 透传，处理器里 `e.preventDefault()` 后 `setMenu({ x: e.clientX, y: e.clientY, p, t })`。
   **`e.preventDefault()` 不能漏**：`src/contextMenu.ts` 那个全局监听靠 `e.defaultPrevented` 判断"应用自己已经处理过这次右键"，漏了就会连带弹出 WKWebView 的原生菜单；
3. 渲染 `<ContextMenu>`，三项如上；
4. 重命名用就地编辑：`const [editing, setEditing] = useState<string | null>(null)`（存 blockKey），该行改渲染一个 `<input>`，`defaultValue` 是当前显示标题，Enter 提交（`useLibrary.getState().rename(key, value)`）、Escape 取消、blur 也提交；
5. 「在访达中显示」：`revealInFinder(p.cwd).catch((msg) => useHint.getState().show(String(msg)))`——后端 reject 的就是可直接展示的中文错误；
6. 「从列表移除」：`useLibrary.getState().removeSession(blockKey(p.dirName, t.rootKey))`。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/SidebarMenu.test.tsx` → PASS（5 个用例）
Run: `npx vitest run` → 全绿（比 Task 6 结束时多 5）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/components/Sidebar.tsx src/App.css src/__tests__/SidebarMenu.test.tsx
git commit -m "feat(sidebar): 右键菜单——重命名、在访达中显示、从列表移除"
```

---

### Task 8: 主页右键菜单（在访达中显示 / 隐藏项目）+ 可撤销提示

**Files:**
- Modify: `src/components/HomePage.tsx`
- Modify: `src/store/hint.ts`
- Modify: `src/App.tsx`（提示的渲染要能承载一个按钮）
- Modify: `src/App.css`
- Test: `src/__tests__/HomePageMenu.test.tsx`（新建）、`src/__tests__/hint.test.ts`（新建）

**Interfaces:**
- Produces: `useHint` 的 `show` 扩展为 `show(msg: string, action?: { label: string; onClick: () => void }): void`；state 增加 `action: { label: string; onClick: () => void } | null`

**⚠️ 有一个会让功能"看起来做了其实点不动"的坑：**

`App.css:174` 的 `.pane-hint` 带 **`pointer-events: none`**。撤销按钮放进去会**完全点不动**，而且没有任何报错——鼠标事件直接穿过去了。

处置：容器**保持** `pointer-events: none`（它是一条浮在终端上方的提示，不该拦截终端的点击），只给按钮加 `pointer-events: auto`。

- [ ] **Step 1: 写失败的测试**

新建 `src/__tests__/hint.test.ts`：

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { useHint } from '../store/hint'

beforeEach(() => { useHint.setState({ message: null, action: null }) })

describe('useHint 的动作支持', () => {
  it('不带动作时 action 为 null（既有调用方不受影响）', () => {
    useHint.getState().show('最多支持 3 个窗格')
    expect(useHint.getState().message).toBe('最多支持 3 个窗格')
    expect(useHint.getState().action).toBeNull()
  })
  it('带动作时 action 可读且可调用', () => {
    const onClick = vi.fn()
    useHint.getState().show('已隐藏 aTerm', { label: '撤销', onClick })
    expect(useHint.getState().action?.label).toBe('撤销')
    useHint.getState().action!.onClick()
    expect(onClick).toHaveBeenCalled()
  })
  it('超时后 message 与 action 一起清空——只清一个会留下一个孤零零的按钮', () => {
    vi.useFakeTimers()
    useHint.getState().show('已隐藏 aTerm', { label: '撤销', onClick: vi.fn() })
    vi.advanceTimersByTime(2300)
    expect(useHint.getState().message).toBeNull()
    expect(useHint.getState().action).toBeNull()
    vi.useRealTimers()
  })
})
```

新建 `src/__tests__/HomePageMenu.test.tsx`。**mock 块直接照抄 `src/__tests__/HomePage.test.tsx`
现有的那一份**（`HomePage` 会渲染 `<HooksPromptBar/>`，同样需要 `store/hooksInstall` 的三个导出
齐全，理由见 Task 6 的注释），并在 `../ipc` 的 mock 里额外加上 `revealInFinder: vi.fn(async () => {})`：

```tsx
describe('主页项目右键菜单', () => {
  it('右键项目卡片弹出两项', () => {
    render(<HomePage />)
    fireEvent.contextMenu(screen.getByText('a'))     // 项目名（basename of /tmp/a）
    expect(screen.queryByText('在访达中显示')).toBeTruthy()
    expect(screen.queryByText('隐藏项目')).toBeTruthy()
  })

  it('「隐藏项目」后该项目从主页消失', async () => {
    render(<HomePage />)
    fireEvent.contextMenu(screen.getByText('a'))
    fireEvent.click(screen.getByText('隐藏项目'))
    await waitFor(() => expect(screen.queryByText('a')).toBeNull())
  })

  it('隐藏后给出可撤销提示，点撤销该项目回来', async () => {
    render(<HomePage />)
    fireEvent.contextMenu(screen.getByText('a'))
    fireEvent.click(screen.getByText('隐藏项目'))
    // 撤销动作被登记进 useHint
    const action = useHint.getState().action
    expect(action?.label).toBe('撤销')
    action!.onClick()
    await waitFor(() => expect(screen.queryByText('a')).toBeTruthy())
  })

  it('搜索结果不做隐藏过滤——明确搜出来的东西还藏起来只会让人以为坏了', async () => {
    render(<HomePage />)
    fireEvent.contextMenu(screen.getByText('a'))
    fireEvent.click(screen.getByText('隐藏项目'))
    await waitFor(() => expect(screen.queryByText('a')).toBeNull())
    fireEvent.change(screen.getByPlaceholderText(/搜索/), { target: { value: '修登录' } })
    await waitFor(() => expect(screen.queryByText('修登录')).toBeTruthy())
  })
})
```

同样，`beforeEach` 里要 `useLibrary.setState({ aliases: {}, hiddenProjects: {}, removedSessions: {} })` 与 `useHint.setState({ message: null, action: null })`。

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/hint.test.ts src/__tests__/HomePageMenu.test.tsx`
Expected: FAIL —— `action` 字段不存在；右键无菜单。

- [ ] **Step 3: 实现**

`src/store/hint.ts`：

```ts
type HintAction = { label: string; onClick: () => void }
type HintState = {
  message: string | null
  action: HintAction | null
  show(msg: string, action?: HintAction): void
}

export const useHint = create<HintState>((set) => ({
  message: null,
  action: null,
  show: (msg, action) => {
    if (timer) clearTimeout(timer)
    set({ message: msg, action: action ?? null })
    // message 与 action 必须一起清空：只清 message 会在屏幕上留下一个没有文字、
    // 却仍然可点的按钮。
    timer = setTimeout(() => set({ message: null, action: null }), HINT_DURATION_MS)
  },
}))
```

`src/App.tsx` 的提示渲染（约 258 行）：

```tsx
{hint && (
  <div className="pane-hint">
    {hint}
    {hintAction && (
      <button type="button" className="pane-hint-action" onClick={() => { hintAction.onClick(); useHint.setState({ message: null, action: null }) }}>
        {hintAction.label}
      </button>
    )}
  </div>
)}
```
（`const hintAction = useHint((s) => s.action)`）

`src/App.css`：

```css
/* .pane-hint 整体保持 pointer-events: none——它浮在终端上方，不该拦截终端的点击。
   只有这个按钮需要能被点到，所以单独放开。漏了这一行，撤销按钮会完全点不动，
   且没有任何报错：鼠标事件直接穿过去。 */
.pane-hint-action { pointer-events: auto; margin-left: 10px; padding: 2px 8px; border-radius: 6px;
  border: 1px solid var(--color-border); background: var(--color-panel); color: var(--color-accent);
  font-size: 12.5px; cursor: default; }
.pane-hint-action:hover { border-color: var(--color-accent); }
```

`HomePage.tsx`：

1. 卡片列表过滤掉 `hiddenProjects` 里的项目——**只过滤卡片视图，搜索结果不过滤**；
2. `ProjectCard` 加 `onContextMenu`（记得 `e.preventDefault()`，理由同 Task 7），弹 `ContextMenu` 两项；
3. 「隐藏项目」：
   ```ts
   useLibrary.getState().hideProject(p.dirName)
   useHint.getState().show(`已隐藏 ${basename(p.cwd)}`, {
     label: '撤销',
     onClick: () => useLibrary.getState().unhideProject(p.dirName),
   })
   ```
4. 「在访达中显示」：同 Task 7 的写法。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/__tests__/hint.test.ts src/__tests__/HomePageMenu.test.tsx` → PASS（3 + 4 = 7 个用例）
Run: `npx vitest run` → 全绿（比 Task 7 结束时多 7）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/components/HomePage.tsx src/store/hint.ts src/App.tsx src/App.css src/__tests__/
git commit -m "feat(home): 项目右键菜单——在访达中显示、隐藏（带可撤销提示）

.pane-hint 整体是 pointer-events:none，撤销按钮单独放开，否则点不动。"
```

---

### Task 9: 收尾两项（`resumeThread` 传 sessionId、三处列表统一走 `displayTitle`）

**Files:**
- Modify: `src/actions.ts`
- Modify: `src/components/HomePage.tsx`、`src/components/OverviewPage.tsx`
- Test: `src/__tests__/actions.test.ts`（追加）

**Interfaces:**
- Consumes: `displayTitle`（Task 1）、`useLibrary`（Task 2）

- [ ] **Step 1: 写失败的测试**

在 `src/__tests__/actions.test.ts` 追加：

```ts
describe('resumeThread 也带上 sessionId', () => {
  it('传给 openTerminal 的参数里含 sessionId，且等于 resumeSessionId', async () => {
    // 为什么需要：被 resume 的链如果此前没有用户消息，rootKey 就等于 session_id；
    // 用户发出第一句话后 rootKey 翻成那条消息的 uuid，此时窗格若没有 sessionId
    // 就再也对不上账，永久失联。resumeSessionId 必在该链的 sessionIds 里。
    const spy = vi.spyOn(useTabs.getState(), 'openTerminal').mockResolvedValue(undefined)
    await resumeThread('-tmp-a', '/tmp/a', makeThread({ rootKey: 'r1', resumeSessionId: 's-newest' }))
    expect(spy).toHaveBeenCalledWith(expect.objectContaining({ sessionId: 's-newest' }))
    spy.mockRestore()
  })
})
```

（该文件已有 `vi.mock('../ipc', …)` 与 `vi.mock('../ptyBuffer', …)`，沿用即可；需要 import `resumeThread`、`useTabs`、`makeThread`。）

- [ ] **Step 2: 运行测试确认它失败**

Run: `npx vitest run src/__tests__/actions.test.ts`
Expected: FAIL —— 参数里没有 `sessionId`。

- [ ] **Step 3: 实现**

`src/actions.ts` 的 `resumeThread`，在传给 `openTerminal` 的对象里加一个字段：

```ts
  return useTabs.getState().openTerminal({
    title: t.title, cwd, inject: `claude --resume ${t.resumeSessionId}`,
    threadKey, dirName, rootKey: t.rootKey,
    // resumeSessionId 必在该链的 sessionIds 里，所以带上它之后，--resume 起的窗格
    // 也能被 reconcilePanes 对账——修掉「被 resume 的链此前无用户消息、发第一句话
    // 后 rootKey 翻转导致该窗格永久失联」这个缺口（V3.0 终审留账）。
    sessionId: t.resumeSessionId,
  })
```

`HomePage.tsx` / `OverviewPage.tsx`：把直接渲染 `t.title` 的地方改成 `displayTitle(t, dirName, aliases)`（`aliases` 取自 `useLibrary`）。**先 grep 找全**：

```bash
grep -n "\.title" src/components/HomePage.tsx src/components/OverviewPage.tsx
```

注意区分：项目名（`basename(p.cwd)`）不要改，只改会话标题。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run` → 全绿（比 Task 8 结束时多 1）
Run: `npm run build`

- [ ] **Step 5: 提交**

```bash
git add src/actions.ts src/components/HomePage.tsx src/components/OverviewPage.tsx src/__tests__/actions.test.ts
git commit -m "fix: resumeThread 补传 sessionId；三处列表统一走 displayTitle

两笔都是 V3.0 终审留下的账。"
```

---

## 收尾

全部任务完成后：

- [ ] `npx vitest run` 与 `cargo test --manifest-path src-tauri/Cargo.toml` 全绿（前端约 653 + 38，Rust 115）
- [ ] `npm run build` 成功
- [ ] 用 `superpowers:finishing-a-development-branch` 收尾
- [ ] 真机验收清单交给用户：
  1. 侧栏会话超过 12 条时全部可见、滚动正常、按今天/昨天/更早分组
  2. 单击只高亮不打开；双击才打开
  3. 右键会话：重命名（回车生效、Esc 取消）、在访达中显示、从列表移除
  4. **被移除的会话再次活动后自动回到列表**
  5. 右键主页项目：在访达中显示、隐藏；隐藏后**点提示里的「撤销」能点得动**并恢复
  6. 隐藏后在搜索框里搜该项目，**仍应搜得到**
  7. 点标签栏 `＋` 弹出选择器；⌘T 仍然直接开空终端
  8. 总览页里改过的会话名**在侧栏和主页也显示**，且升级后**原有的名字没有丢**
