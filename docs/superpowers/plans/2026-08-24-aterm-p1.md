# aTerm P1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 可日常使用的 macOS 终端：真实 zsh 终端标签 + 启动器式主页 + 侧边栏 + 发现并一键恢复任意 Claude Code 历史对话（含会话链去重）+ 新建对话。

**Architecture:** Tauri 2 应用。Rust 核心提供两组能力：(1) PTY 管理（portable-pty，每个终端标签一个真实 `/bin/zsh -l`，输出经 Tauri 事件以 base64 推给前端）；(2) 会话索引（只读扫描 `~/.claude/projects/`，每文件仅读头尾，按"首条用户消息 uuid"串会话链）。前端 React + zustand + xterm.js，单窗口：左侧边栏 + 顶部标签条 + 内容区（主页 / 终端）。

**Tech Stack:** Tauri 2 (Rust)、React 18 + TypeScript + Vite、xterm.js (`@xterm/xterm` + `@xterm/addon-fit`)、zustand、portable-pty、serde_json、chrono、base64、dirs、vitest + @testing-library/react、cargo test + tempfile。

**Spec:** `docs/superpowers/specs/2026-08-24-aterm-design.md`（P1 范围见 §12 分期表；本计划实现 P1 一行）

## Global Constraints

- 仅 macOS；Tauri 2；identifier `com.hou.aterm`；productName `aTerm`
- **对 `~/.claude/` 绝对只读**——P1 不写入该目录任何内容（hooks 是 P2）
- 会话文件**永不全量读取**：头部 ≤40 行且 ≤256KB，尾部 ≤64KB（spec §6.1）
- P1 不做：文件监听、状态颜色、总览方块页、重命名、hooks（全部 P2+）；主页/侧边栏只显示静态信息（标题+时间）
- UI 文案为中文（按 spec §5 已确认样式：暗色主题）
- 逻辑模块（解析、串链、扫描、PTY 核心、store、时间格式化）一律 TDD：先写失败测试再实现
- 每个 commit message 结尾加：`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 若第三方 crate/包的 API 签名与计划中代码有出入，以编译器/官方文档为准做最小调整，不改变本计划定义的模块间接口

## File Structure

```
aTerm/
├─ package.json / vite.config.ts / tsconfig.json      # Task 1 脚手架
├─ src-tauri/
│  ├─ tauri.conf.json                                  # Task 1（名称/窗口/withGlobalTauri）
│  ├─ capabilities/default.json                        # Task 1（加 dialog:default）
│  ├─ Cargo.toml                                       # Task 1（加依赖）
│  └─ src/
│     ├─ lib.rs                # Builder 装配：state + invoke_handler（Task 4/5 修改）
│     ├─ sessions/mod.rs       # pub mod parser; pub mod scan;
│     ├─ sessions/parser.rs    # Task 2：头尾行读取 + 单文件元数据解析
│     ├─ sessions/scan.rs      # Task 3：扫描项目 + 会话链分组；Task 4：list_projects 命令
│     ├─ pty_core.rs           # Task 5：纯 PTY 封装（可脱离 Tauri 单测）
│     └─ pty.rs                # Task 5：Tauri 命令层 + 事件发射
└─ src/
   ├─ ipc.ts                   # Task 6：类型 + invoke 封装（前后端接口契约）
   ├─ b64.ts / time.ts         # Task 6：base64 解码、相对时间
   ├─ store/tabs.ts            # Task 7：标签页 store（zustand）
   ├─ store/sessions.ts        # Task 7：项目/会话数据 store
   ├─ actions.ts               # Task 9：resumeThread / newConversation / runCommand
   ├─ components/TabBar.tsx    # Task 8
   ├─ components/TerminalView.tsx # Task 8：xterm 封装
   ├─ components/Sidebar.tsx   # Task 9
   ├─ components/HomePage.tsx  # Task 9
   ├─ App.tsx / App.css        # Task 8 骨架，Task 9 完善
   └─ __tests__/               # time.test.ts tabs.test.ts HomePage.test.tsx
```

---

### Task 1: 脚手架——Tauri 2 + React-TS 落进仓库根目录

**Files:**
- Create: 整个 Tauri+Vite 工程（脚手架生成后并入根目录）
- Modify: `src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/capabilities/default.json`、`vite.config.ts`、`.gitignore`

**Interfaces:**
- Consumes: 无
- Produces: 可运行的空应用；`npm run tauri dev` 打开窗口；`cargo test`/`npx vitest run` 可跑（0 测试通过）

- [ ] **Step 1: 确认前置工具**

Run: `cargo --version && node --version && npm --version`
Expected: 三者都有输出（Rust ≥1.77，Node ≥18）。缺 Rust 则先 `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y` 并 `source "$HOME/.cargo/env"`。

- [ ] **Step 2: 生成脚手架并并入根目录**

```bash
cd /Users/hou.astro/aTerm
npm create tauri-app@latest aterm-scaffold -- --manager npm --template react-ts --yes
# 若 --yes 不被该版本支持，交互式回答：name=aterm-scaffold, frontend=TypeScript, manager=npm, UI=React, flavor=TypeScript
rsync -a aterm-scaffold/ ./ --exclude node_modules
rm -rf aterm-scaffold
npm install
```

- [ ] **Step 3: 安装依赖**

```bash
npm install @xterm/xterm @xterm/addon-fit zustand @tauri-apps/plugin-dialog
npm install -D vitest jsdom @testing-library/react @testing-library/user-event
cd src-tauri && cargo add portable-pty base64 dirs chrono tauri-plugin-dialog && cargo add --dev tempfile && cd ..
```

- [ ] **Step 4: 配置**

`src-tauri/tauri.conf.json` 改以下键（其余保持模板值）：
```json
{
  "productName": "aTerm",
  "identifier": "com.hou.aterm",
  "app": {
    "withGlobalTauri": true,
    "windows": [{ "title": "aTerm", "width": 1200, "height": 780, "minWidth": 800, "minHeight": 500 }]
  }
}
```
`src-tauri/capabilities/default.json` 的 `permissions` 数组追加 `"dialog:default"`。
`src-tauri/src/lib.rs` 的 Builder 上追加 `.plugin(tauri_plugin_dialog::init())`。
`vite.config.ts` 顶部加 `/// <reference types="vitest" />`，`defineConfig` 内加：
```ts
test: { environment: 'jsdom', passWithNoTests: true },
```
`.gitignore` 追加（若缺）：`node_modules/`、`src-tauri/target/`、`dist/`。

- [ ] **Step 5: 验证**

Run: `cd src-tauri && cargo test && cd .. && npx vitest run && npm run tauri dev`
Expected: cargo test 编译通过 0 failed；vitest "no tests" 通过；桌面窗口打开显示模板页，标题 aTerm。手动关闭窗口。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: Tauri 2 + React-TS 脚手架与依赖

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Rust 会话文件解析器（头尾读取 + 元数据提取）

**Files:**
- Create: `src-tauri/src/sessions/mod.rs`、`src-tauri/src/sessions/parser.rs`
- Modify: `src-tauri/src/lib.rs`（加 `mod sessions;`）

**Interfaces:**
- Consumes: 无
- Produces:
  - `pub struct ParsedMeta { pub first_user_uuid: Option<String>, pub title: Option<String>, pub cwd: Option<String>, pub last_ts_ms: Option<i64> }`
  - `pub fn parse_meta(head: &[String], tail: &[String]) -> ParsedMeta`
  - `pub fn read_head_lines(path: &Path, max_lines: usize, max_bytes: usize) -> std::io::Result<Vec<String>>`
  - `pub fn read_tail_lines(path: &Path, max_bytes: u64) -> std::io::Result<Vec<String>>`

解析规则（防御式：任何一行 JSON 解析失败直接跳过，绝不 panic）：
- `first_user_uuid`：头部第一条满足 `message.role == "user"` 且 `isSidechain != true` 的记录的 `uuid` —— 这是会话链的链键（spec §4：恢复产生的新文件会复制旧消息与 uuid）
- `title` 优先级：① 头部 `type=="summary"` 记录的 `summary` 字段；② 第一条用户消息文本（`message.content` 为字符串直接用；为数组取第一个 `type=="text"` 项的 `text`），跳过以 `<` 或 `Caveat:` 开头的系统注入文本；取首行、截断 60 字符
- `cwd`：头部任意记录的 `cwd` 字段（首个出现者）
- `last_ts_ms`：尾部最后一条含 `timestamp`（RFC3339 字符串）记录，用 chrono 转毫秒

- [ ] **Step 1: 写失败测试**（`parser.rs` 底部 `#[cfg(test)] mod tests`）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::io::Write as _;

    fn line_user(uuid: &str, text: &str, ts: &str) -> String {
        format!(r#"{{"parentUuid":null,"isSidechain":false,"cwd":"/tmp/fake-proj","sessionId":"s","timestamp":"{ts}","type":"user","message":{{"role":"user","content":"{text}"}},"uuid":"{uuid}"}}"#)
    }

    #[test]
    fn title_prefers_summary_entry() {
        let head = vec![
            r#"{"type":"summary","summary":"修复登录","leafUuid":"x"}"#.to_string(),
            line_user("u1", "帮我修复登录 bug", "2026-08-20T10:00:00.000Z"),
        ];
        let m = parse_meta(&head, &[]);
        assert_eq!(m.title.as_deref(), Some("修复登录"));
        assert_eq!(m.first_user_uuid.as_deref(), Some("u1"));
        assert_eq!(m.cwd.as_deref(), Some("/tmp/fake-proj"));
    }

    #[test]
    fn title_falls_back_to_first_user_message_skipping_injected() {
        let head = vec![
            r#"{"type":"mode","mode":"normal","sessionId":"s"}"#.to_string(),
            line_user("u0", "<command-name>/clear</command-name>", "2026-08-20T09:00:00.000Z"),
            line_user("u1", "写一个贪吃蛇", "2026-08-20T10:00:00.000Z"),
        ];
        let m = parse_meta(&head, &[]);
        assert_eq!(m.title.as_deref(), Some("写一个贪吃蛇"));
        // 链键仍是第一条真实用户消息（含被跳过标题的那条）
        assert_eq!(m.first_user_uuid.as_deref(), Some("u0"));
    }

    #[test]
    fn last_ts_from_tail_and_garbage_tolerated() {
        let tail = vec![
            "not-json-at-all".to_string(),
            r#"{"type":"assistant","uuid":"u9","timestamp":"2026-08-21T08:30:00.000Z","message":{"role":"assistant","model":"claude-opus-5"}}"#.to_string(),
        ];
        let m = parse_meta(&[], &tail);
        let expected = chrono::DateTime::parse_from_rfc3339("2026-08-21T08:30:00.000Z").unwrap().timestamp_millis();
        assert_eq!(m.last_ts_ms, Some(expected));
        assert!(m.title.is_none());
    }

    #[test]
    fn head_and_tail_file_reads_are_bounded() {
        let mut f = tempfile::NamedTempFile::new().unwrap();
        for i in 0..100 { writeln!(f, r#"{{"n":{i}}}"#).unwrap(); }
        let head = read_head_lines(f.path(), 5, 1024).unwrap();
        assert_eq!(head.len(), 5);
        assert!(head[0].contains(r#""n":0"#));
        let tail = read_tail_lines(f.path(), 40).unwrap(); // 40 字节必然截断出部分行
        assert!(tail.iter().all(|l| l.starts_with('{')), "首个不完整行必须被丢弃: {tail:?}");
        assert!(tail.last().unwrap().contains("99"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test sessions::parser`
Expected: 编译错误（模块/函数不存在）。

- [ ] **Step 3: 最小实现**

`src-tauri/src/sessions/mod.rs`：
```rust
pub mod parser;
pub mod scan;
```
（`scan.rs` 先建空文件占位，内容 Task 3 填。）

`src-tauri/src/sessions/parser.rs`：
```rust
use serde_json::Value;
use std::io::{BufRead, BufReader, Read, Seek, SeekFrom};
use std::path::Path;

#[derive(Debug, Default, PartialEq)]
pub struct ParsedMeta {
    pub first_user_uuid: Option<String>,
    pub title: Option<String>,
    pub cwd: Option<String>,
    pub last_ts_ms: Option<i64>,
}

fn as_str(v: &Value, key: &str) -> Option<String> {
    v.get(key).and_then(|x| x.as_str()).map(|s| s.to_string())
}

fn user_text(v: &Value) -> Option<String> {
    let msg = v.get("message")?;
    if msg.get("role")?.as_str()? != "user" { return None; }
    let content = msg.get("content")?;
    if let Some(s) = content.as_str() { return Some(s.to_string()); }
    content.as_array()?.iter()
        .find(|item| item.get("type").and_then(|t| t.as_str()) == Some("text"))
        .and_then(|item| item.get("text")).and_then(|t| t.as_str()).map(|s| s.to_string())
}

fn is_injected(text: &str) -> bool {
    let t = text.trim_start();
    t.starts_with('<') || t.starts_with("Caveat:")
}

fn truncate_chars(s: &str, max: usize) -> String {
    s.chars().take(max).collect()
}

pub fn parse_meta(head: &[String], tail: &[String]) -> ParsedMeta {
    let mut m = ParsedMeta::default();
    for line in head {
        let Ok(v) = serde_json::from_str::<Value>(line) else { continue };
        if m.cwd.is_none() { m.cwd = as_str(&v, "cwd"); }
        if m.title.is_none() && v.get("type").and_then(|t| t.as_str()) == Some("summary") {
            m.title = as_str(&v, "summary");
        }
        let sidechain = v.get("isSidechain").and_then(|b| b.as_bool()).unwrap_or(false);
        if !sidechain {
            if let Some(text) = user_text(&v) {
                if m.first_user_uuid.is_none() { m.first_user_uuid = as_str(&v, "uuid"); }
                if m.title.is_none() && !is_injected(&text) {
                    let first_line = text.lines().next().unwrap_or("").trim();
                    if !first_line.is_empty() { m.title = Some(truncate_chars(first_line, 60)); }
                }
            }
        }
    }
    for line in tail {
        let Ok(v) = serde_json::from_str::<Value>(line) else { continue };
        if let Some(ts) = as_str(&v, "timestamp") {
            if let Ok(dt) = chrono::DateTime::parse_from_rfc3339(&ts) {
                m.last_ts_ms = Some(dt.timestamp_millis());
            }
        }
    }
    m
}

pub fn read_head_lines(path: &Path, max_lines: usize, max_bytes: usize) -> std::io::Result<Vec<String>> {
    let f = std::fs::File::open(path)?;
    let limited = BufReader::new(f).take(max_bytes as u64);
    Ok(limited.lines().map_while(Result::ok).take(max_lines).collect())
}

pub fn read_tail_lines(path: &Path, max_bytes: u64) -> std::io::Result<Vec<String>> {
    let mut f = std::fs::File::open(path)?;
    let len = f.metadata()?.len();
    let start = len.saturating_sub(max_bytes);
    f.seek(SeekFrom::Start(start))?;
    let mut buf = String::new();
    f.read_to_string(&mut buf).ok(); // 非 UTF-8 边界容忍：失败则返回空
    let mut lines: Vec<String> = buf.lines().map(|s| s.to_string()).collect();
    if start > 0 && !lines.is_empty() { lines.remove(0); } // 丢弃被截断的首行
    Ok(lines)
}
```
`src-tauri/src/lib.rs` 顶部加 `mod sessions;`（还有 Task 5 会加 `mod pty; mod pty_core;`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test sessions::parser`
Expected: 4 passed。

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src
git commit -m "feat(core): 会话文件头尾解析器（标题/链键/cwd/最后活动时间）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 扫描器 + 会话链分组

**Files:**
- Create: `src-tauri/src/sessions/scan.rs`（替换占位）
- Test: 同文件 `#[cfg(test)]`

**Interfaces:**
- Consumes: Task 2 的 `parser::{parse_meta, read_head_lines, read_tail_lines, ParsedMeta}`
- Produces:
  - `pub struct ThreadInfo { pub root_key: String, pub resume_session_id: String, pub title: String, pub cwd: String, pub last_activity_ms: i64, pub file_count: u32 }`（derive `Serialize, Clone, Debug, PartialEq`，`#[serde(rename_all = "camelCase")]`）
  - `pub struct ProjectInfo { pub dir_name: String, pub cwd: String, pub last_activity_ms: i64, pub threads: Vec<ThreadInfo> }`（同上 derive）
  - `pub fn scan_projects(projects_dir: &Path) -> Vec<ProjectInfo>`

规则：
- 只取项目目录顶层 `*.jsonl` 且文件名主干形如 UUID（36 字符含 4 个 `-`）——排除 `agent-*.jsonl` 等子代理文件
- 链键 = `first_user_uuid`，取不到则退化为该文件自己的 session id（永不合并错）
- 每链：按 `last_activity_ms`（取不到 timestamp 用文件 mtime）排序；`resume_session_id` = 最新文件的文件名主干；`title` = 最新的非空 title，全空则 session id 前 8 位；链的 `last_activity_ms` = 最新文件的
- 项目 `cwd` = 最新线程的 cwd，全取不到则由目录名 `-`→`/` 粗略还原（仅展示用）
- 项目内线程按时间降序；项目列表按各自最新活动降序；无有效 jsonl 的目录跳过

- [ ] **Step 1: 写失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;

    const S_A: &str = "11111111-1111-1111-1111-111111111111"; // 原始会话
    const S_B: &str = "22222222-2222-2222-2222-222222222222"; // A 的恢复（同 first_user_uuid）
    const S_C: &str = "33333333-3333-3333-3333-333333333333"; // 独立会话

    fn write_session(dir: &std::path::Path, sid: &str, first_uuid: &str, text: &str, ts: &str) {
        let l1 = format!(r#"{{"parentUuid":null,"isSidechain":false,"cwd":"/tmp/fake-proj","sessionId":"{sid}","timestamp":"{ts}","type":"user","message":{{"role":"user","content":"{text}"}},"uuid":"{first_uuid}"}}"#);
        let l2 = format!(r#"{{"type":"assistant","uuid":"{first_uuid}-a","timestamp":"{ts}","sessionId":"{sid}","message":{{"role":"assistant","model":"claude-opus-5"}}}}"#);
        fs::write(dir.join(format!("{sid}.jsonl")), format!("{l1}\n{l2}\n")).unwrap();
    }

    #[test]
    fn resumed_session_merges_into_one_thread() {
        let tmp = tempfile::tempdir().unwrap();
        let proj = tmp.path().join("-tmp-fake-proj");
        fs::create_dir(&proj).unwrap();
        write_session(&proj, S_A, "u-root", "修复登录 bug", "2026-08-20T10:00:00.000Z");
        write_session(&proj, S_B, "u-root", "修复登录 bug", "2026-08-21T09:00:00.000Z"); // 恢复：链键相同、更新
        write_session(&proj, S_C, "u-other", "写贪吃蛇", "2026-08-19T08:00:00.000Z");
        fs::write(proj.join("agent-xyz.jsonl"), "{}\n").unwrap(); // 必须被忽略

        let out = scan_projects(tmp.path());
        assert_eq!(out.len(), 1);
        let p = &out[0];
        assert_eq!(p.cwd, "/tmp/fake-proj");
        assert_eq!(p.threads.len(), 2, "A+B 应合并为一条链");
        let t0 = &p.threads[0]; // 降序第一 = 登录链
        assert_eq!(t0.root_key, "u-root");
        assert_eq!(t0.resume_session_id, S_B, "恢复目标必须是链上最新文件");
        assert_eq!(t0.file_count, 2);
        assert_eq!(p.threads[1].title, "写贪吃蛇");
    }

    #[test]
    fn empty_or_invalid_dirs_skipped() {
        let tmp = tempfile::tempdir().unwrap();
        fs::create_dir(tmp.path().join("-empty")).unwrap();
        assert!(scan_projects(tmp.path()).is_empty());
        assert!(scan_projects(&tmp.path().join("nonexistent")).is_empty());
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test sessions::scan`
Expected: 编译错误（类型/函数不存在）。

- [ ] **Step 3: 最小实现**

```rust
use super::parser::{parse_meta, read_head_lines, read_tail_lines};
use serde::Serialize;
use std::path::Path;

#[derive(Debug, Clone, Serialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct ThreadInfo {
    pub root_key: String,
    pub resume_session_id: String,
    pub title: String,
    pub cwd: String,
    pub last_activity_ms: i64,
    pub file_count: u32,
}

#[derive(Debug, Clone, Serialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct ProjectInfo {
    pub dir_name: String,
    pub cwd: String,
    pub last_activity_ms: i64,
    pub threads: Vec<ThreadInfo>,
}

fn is_uuid_stem(stem: &str) -> bool {
    stem.len() == 36 && stem.chars().filter(|c| *c == '-').count() == 4
        && stem.chars().all(|c| c.is_ascii_hexdigit() || c == '-')
}

struct FileMeta { session_id: String, meta: super::parser::ParsedMeta, mtime_ms: i64 }

pub fn scan_projects(projects_dir: &Path) -> Vec<ProjectInfo> {
    let Ok(entries) = std::fs::read_dir(projects_dir) else { return vec![] };
    let mut projects: Vec<ProjectInfo> = vec![];
    for entry in entries.flatten() {
        let dir = entry.path();
        if !dir.is_dir() { continue; }
        let mut files: Vec<FileMeta> = vec![];
        let Ok(inner) = std::fs::read_dir(&dir) else { continue };
        for f in inner.flatten() {
            let p = f.path();
            if p.extension().and_then(|e| e.to_str()) != Some("jsonl") { continue; }
            let Some(stem) = p.file_stem().and_then(|s| s.to_str()) else { continue };
            if !is_uuid_stem(stem) { continue; }
            let head = read_head_lines(&p, 40, 256 * 1024).unwrap_or_default();
            let tail = read_tail_lines(&p, 64 * 1024).unwrap_or_default();
            let mtime_ms = f.metadata().ok()
                .and_then(|m| m.modified().ok())
                .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                .map(|d| d.as_millis() as i64).unwrap_or(0);
            files.push(FileMeta { session_id: stem.to_string(), meta: parse_meta(&head, &tail), mtime_ms });
        }
        if files.is_empty() { continue; }

        let mut groups: std::collections::HashMap<String, Vec<&FileMeta>> = Default::default();
        for fm in &files {
            let key = fm.meta.first_user_uuid.clone().unwrap_or_else(|| fm.session_id.clone());
            groups.entry(key).or_default().push(fm);
        }
        let mut threads: Vec<ThreadInfo> = groups.into_iter().map(|(key, mut fs)| {
            fs.sort_by_key(|fm| fm.meta.last_ts_ms.unwrap_or(fm.mtime_ms));
            let newest = fs.last().unwrap();
            let title = fs.iter().rev().find_map(|fm| fm.meta.title.clone())
                .unwrap_or_else(|| newest.session_id.chars().take(8).collect());
            let cwd = fs.iter().rev().find_map(|fm| fm.meta.cwd.clone()).unwrap_or_default();
            ThreadInfo {
                root_key: key,
                resume_session_id: newest.session_id.clone(),
                title, cwd,
                last_activity_ms: newest.meta.last_ts_ms.unwrap_or(newest.mtime_ms),
                file_count: fs.len() as u32,
            }
        }).collect();
        threads.sort_by_key(|t| std::cmp::Reverse(t.last_activity_ms));

        let dir_name = dir.file_name().and_then(|s| s.to_str()).unwrap_or("").to_string();
        let cwd = threads.iter().find(|t| !t.cwd.is_empty()).map(|t| t.cwd.clone())
            .unwrap_or_else(|| dir_name.replace('-', "/"));
        projects.push(ProjectInfo {
            dir_name,
            cwd,
            last_activity_ms: threads.first().map(|t| t.last_activity_ms).unwrap_or(0),
            threads,
        });
    }
    projects.sort_by_key(|p| std::cmp::Reverse(p.last_activity_ms));
    projects
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test sessions`
Expected: 6 passed（含 Task 2 的 4 个）。

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/sessions
git commit -m "feat(core): 项目扫描与会话链分组（恢复文件合并、agent 文件排除）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: `list_projects` Tauri 命令

**Files:**
- Modify: `src-tauri/src/sessions/scan.rs`（追加命令函数）、`src-tauri/src/lib.rs`（注册）

**Interfaces:**
- Consumes: `scan_projects`
- Produces: 前端可 `invoke('list_projects')` → `Vec<ProjectInfo>`（camelCase JSON）

- [ ] **Step 1: 实现命令**（薄封装，逻辑已被 Task 3 测试覆盖）

`scan.rs` 追加：
```rust
#[tauri::command]
pub fn list_projects() -> Result<Vec<ProjectInfo>, String> {
    let home = dirs::home_dir().ok_or("找不到用户目录")?;
    Ok(scan_projects(&home.join(".claude").join("projects")))
}
```
`lib.rs` 的 Builder：`.invoke_handler(tauri::generate_handler![sessions::scan::list_projects])`（模板若有 `greet` 命令，删除之及其前端调用）。

- [ ] **Step 2: 真机烟测**

Run: `npm run tauri dev`，窗口打开后在 WebView 控制台（右键→检查元素→Console）执行：
```js
await window.__TAURI__.core.invoke('list_projects')
```
Expected: 返回数组，包含你机器上的真实项目（如 `-Users-hou-astro-phineuro`），threads 有 title/resumeSessionId/lastActivityMs，且**同一对话不重复出现**。

- [ ] **Step 3: Commit**

```bash
git add src-tauri/src
git commit -m "feat(core): list_projects 命令接通前端

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: PTY 核心与 Tauri 命令层

**Files:**
- Create: `src-tauri/src/pty_core.rs`、`src-tauri/src/pty.rs`
- Modify: `src-tauri/src/lib.rs`（模块、state、命令注册）

**Interfaces:**
- Consumes: 无
- Produces（pty_core，纯 Rust 可单测）:
  - `pub struct PtyHandle`，方法：`write(&self, bytes: &[u8]) -> Result<(), String>`、`resize(&self, cols: u16, rows: u16) -> Result<(), String>`、`kill(&self)`、`is_alive(&self) -> bool`
  - `pub fn spawn(program: &str, args: &[String], cwd: Option<&Path>, inject: Option<&str>, cols: u16, rows: u16, on_output: Box<dyn FnMut(&[u8]) + Send>, on_exit: Box<dyn FnOnce(u32) + Send>) -> Result<PtyHandle, String>`
- Produces（pty.rs，Tauri 层）：命令 `pty_spawn(cwd: Option<String>, inject: Option<String>, cols: u16, rows: u16) -> String（ptyId）`、`pty_write(id, data: String)`、`pty_resize(id, cols, rows)`、`pty_kill(id)`、`pty_is_alive(id) -> bool`；事件 `pty-output` 载荷 `{ id: String, data: String /* base64 */ }`、`pty-exit` 载荷 `{ id: String, code: u32 }`

- [ ] **Step 1: 写失败测试**（`pty_core.rs` 底部）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::mpsc;
    use std::time::Duration;

    #[test]
    fn spawn_echo_captures_output_and_exit() {
        let (tx, rx) = mpsc::channel::<Vec<u8>>();
        let (etx, erx) = mpsc::channel::<u32>();
        let h = spawn("/bin/echo", &["hello-pty".to_string()], None, None, 80, 24,
            Box::new(move |b| { let _ = tx.send(b.to_vec()); }),
            Box::new(move |c| { let _ = etx.send(c); }),
        ).unwrap();
        let code = erx.recv_timeout(Duration::from_secs(5)).expect("应收到退出事件");
        assert_eq!(code, 0);
        std::thread::sleep(Duration::from_millis(200)); // 等 reader 清空
        let mut all = Vec::new();
        while let Ok(chunk) = rx.try_recv() { all.extend(chunk); }
        assert!(String::from_utf8_lossy(&all).contains("hello-pty"));
        assert!(!h.is_alive());
    }

    #[test]
    fn inject_writes_initial_command() {
        let (tx, rx) = mpsc::channel::<Vec<u8>>();
        let (etx, erx) = mpsc::channel::<u32>();
        // cat 会把注入的 stdin 回显出来后被 kill
        let h = spawn("/bin/cat", &[], None, Some("marker-123"), 80, 24,
            Box::new(move |b| { let _ = tx.send(b.to_vec()); }),
            Box::new(move |c| { let _ = etx.send(c); }),
        ).unwrap();
        let deadline = std::time::Instant::now() + Duration::from_secs(5);
        let mut all = Vec::new();
        while std::time::Instant::now() < deadline {
            while let Ok(chunk) = rx.try_recv() { all.extend(chunk); }
            if String::from_utf8_lossy(&all).contains("marker-123") { break; }
            std::thread::sleep(Duration::from_millis(50));
        }
        assert!(String::from_utf8_lossy(&all).contains("marker-123"));
        h.kill();
        let _ = erx.recv_timeout(Duration::from_secs(5)).expect("kill 后应收到退出事件");
        assert!(!h.is_alive());
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test pty_core`
Expected: 编译错误。

- [ ] **Step 3: 实现 pty_core**

```rust
use portable_pty::{native_pty_system, ChildKiller, CommandBuilder, MasterPty, PtySize};
use std::io::{Read, Write};
use std::path::Path;
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::{Arc, Mutex};

pub struct PtyHandle {
    master: Mutex<Box<dyn MasterPty + Send>>,
    writer: Mutex<Box<dyn Write + Send>>,
    killer: Mutex<Box<dyn ChildKiller + Send + Sync>>,
    alive: Arc<AtomicBool>,
}

impl PtyHandle {
    pub fn write(&self, bytes: &[u8]) -> Result<(), String> {
        let mut w = self.writer.lock().map_err(|e| e.to_string())?;
        w.write_all(bytes).and_then(|_| w.flush()).map_err(|e| e.to_string())
    }
    pub fn resize(&self, cols: u16, rows: u16) -> Result<(), String> {
        self.master.lock().map_err(|e| e.to_string())?
            .resize(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })
            .map_err(|e| e.to_string())
    }
    pub fn kill(&self) {
        if let Ok(mut k) = self.killer.lock() { let _ = k.kill(); }
    }
    pub fn is_alive(&self) -> bool { self.alive.load(Ordering::SeqCst) }
}

pub fn spawn(
    program: &str, args: &[String], cwd: Option<&Path>, inject: Option<&str>,
    cols: u16, rows: u16,
    mut on_output: Box<dyn FnMut(&[u8]) + Send>, on_exit: Box<dyn FnOnce(u32) + Send>,
) -> Result<PtyHandle, String> {
    let pty = native_pty_system();
    let pair = pty.openpty(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })
        .map_err(|e| e.to_string())?;
    let mut cmd = CommandBuilder::new(program);
    cmd.args(args);
    cmd.env("TERM", "xterm-256color");
    if std::env::var("LANG").is_err() { cmd.env("LANG", "en_US.UTF-8"); }
    if let Some(d) = cwd { cmd.cwd(d); }
    let mut child = pair.slave.spawn_command(cmd).map_err(|e| e.to_string())?;
    drop(pair.slave);

    let killer = child.clone_killer();
    let mut reader = pair.master.try_clone_reader().map_err(|e| e.to_string())?;
    let mut writer = pair.master.take_writer().map_err(|e| e.to_string())?;
    if let Some(cmdline) = inject {
        writer.write_all(format!("{cmdline}\r").as_bytes()).map_err(|e| e.to_string())?;
        writer.flush().ok();
    }
    let alive = Arc::new(AtomicBool::new(true));

    std::thread::spawn(move || {
        let mut buf = [0u8; 8192];
        loop {
            match reader.read(&mut buf) {
                Ok(0) | Err(_) => break,
                Ok(n) => on_output(&buf[..n]),
            }
        }
    });
    let alive2 = alive.clone();
    std::thread::spawn(move || {
        let code = child.wait().map(|s| s.exit_code()).unwrap_or(1);
        alive2.store(false, Ordering::SeqCst);
        on_exit(code);
    });

    Ok(PtyHandle { master: Mutex::new(pair.master), writer: Mutex::new(writer), killer: Mutex::new(killer), alive })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test pty_core`
Expected: 2 passed。

- [ ] **Step 5: 实现 Tauri 层 `pty.rs`**（薄封装，Task 8 真机验证）

```rust
use crate::pty_core::{self, PtyHandle};
use base64::{engine::general_purpose::STANDARD as B64, Engine as _};
use serde::Serialize;
use std::collections::HashMap;
use std::sync::atomic::{AtomicU64, Ordering};
use std::sync::{Arc, Mutex};
use tauri::{AppHandle, Emitter, Manager, State};

#[derive(Default)]
pub struct PtyManager(pub Mutex<HashMap<String, Arc<PtyHandle>>>);

#[derive(Clone, Serialize)]
struct OutPayload { id: String, data: String }
#[derive(Clone, Serialize)]
struct ExitPayload { id: String, code: u32 }

static NEXT_ID: AtomicU64 = AtomicU64::new(1);

#[tauri::command]
pub fn pty_spawn(app: AppHandle, state: State<PtyManager>, cwd: Option<String>, inject: Option<String>, cols: u16, rows: u16) -> Result<String, String> {
    let id = format!("pty-{}", NEXT_ID.fetch_add(1, Ordering::SeqCst));
    let cwd_path = cwd.map(std::path::PathBuf::from)
        .or_else(|| dirs::home_dir());
    let (a1, a2, id1, id2) = (app.clone(), app.clone(), id.clone(), id.clone());
    let handle = pty_core::spawn(
        "/bin/zsh", &["-l".to_string()], cwd_path.as_deref(), inject.as_deref(), cols, rows,
        Box::new(move |bytes| {
            let _ = a1.emit("pty-output", OutPayload { id: id1.clone(), data: B64.encode(bytes) });
        }),
        Box::new(move |code| {
            a2.state::<PtyManager>().0.lock().ok().map(|mut m| m.remove(&id2));
            let _ = a2.emit("pty-exit", ExitPayload { id: id2.clone(), code });
        }),
    )?;
    state.0.lock().map_err(|e| e.to_string())?.insert(id.clone(), Arc::new(handle));
    Ok(id)
}

fn with_pty<T>(state: &State<PtyManager>, id: &str, f: impl FnOnce(&PtyHandle) -> T) -> Result<T, String> {
    let map = state.0.lock().map_err(|e| e.to_string())?;
    map.get(id).map(|h| f(h)).ok_or_else(|| format!("pty 不存在: {id}"))
}

#[tauri::command]
pub fn pty_write(state: State<PtyManager>, id: String, data: String) -> Result<(), String> {
    with_pty(&state, &id, |h| h.write(data.as_bytes()))?
}
#[tauri::command]
pub fn pty_resize(state: State<PtyManager>, id: String, cols: u16, rows: u16) -> Result<(), String> {
    with_pty(&state, &id, |h| h.resize(cols, rows))?
}
#[tauri::command]
pub fn pty_kill(state: State<PtyManager>, id: String) -> Result<(), String> {
    with_pty(&state, &id, |h| h.kill())
}
#[tauri::command]
pub fn pty_is_alive(state: State<PtyManager>, id: String) -> bool {
    with_pty(&state, &id, |h| h.is_alive()).unwrap_or(false)
}
```
`lib.rs`：加 `mod pty; mod pty_core;`，Builder 加 `.manage(pty::PtyManager::default())`，命令注册扩为：
```rust
.invoke_handler(tauri::generate_handler![
    sessions::scan::list_projects,
    pty::pty_spawn, pty::pty_write, pty::pty_resize, pty::pty_kill, pty::pty_is_alive
])
```

- [ ] **Step 6: 编译 + 全量测试**

Run: `cd src-tauri && cargo test`
Expected: 全部通过（sessions 6 + pty_core 2）。

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src
git commit -m "feat(core): PTY 核心与 spawn/write/resize/kill 命令、输出/退出事件

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 前端基础层——ipc 契约、base64、相对时间

**Files:**
- Create: `src/ipc.ts`、`src/b64.ts`、`src/time.ts`
- Test: `src/__tests__/time.test.ts`（b64 一并测）

**Interfaces:**
- Consumes: Task 4/5 的命令名与载荷
- Produces:
  - `interface ThreadInfo { rootKey: string; resumeSessionId: string; title: string; cwd: string; lastActivityMs: number; fileCount: number }`
  - `interface ProjectInfo { dirName: string; cwd: string; lastActivityMs: number; threads: ThreadInfo[] }`
  - `listProjects(): Promise<ProjectInfo[]>`、`ptySpawn(o: {cwd?: string; inject?: string; cols: number; rows: number}): Promise<string>`、`ptyWrite(id, data)`、`ptyResize(id, cols, rows)`、`ptyKill(id)`、`ptyIsAlive(id): Promise<boolean>`
  - `b64ToBytes(b64: string): Uint8Array`
  - `formatRelative(ms: number, now?: number): string`（刚刚 / n 分钟前 / n 小时前 / 昨天 / n 天前 / YYYY-MM-DD）
  - `basename(p: string): string`

- [ ] **Step 1: 写失败测试**

```ts
// src/__tests__/time.test.ts
import { describe, expect, it } from 'vitest'
import { formatRelative, basename } from '../time'
import { b64ToBytes } from '../b64'

const NOW = Date.parse('2026-08-24T12:00:00Z')
describe('formatRelative', () => {
  it('分档正确', () => {
    expect(formatRelative(NOW - 30_000, NOW)).toBe('刚刚')
    expect(formatRelative(NOW - 5 * 60_000, NOW)).toBe('5 分钟前')
    expect(formatRelative(NOW - 3 * 3600_000, NOW)).toBe('3 小时前')
    expect(formatRelative(NOW - 26 * 3600_000, NOW)).toBe('昨天')
    expect(formatRelative(NOW - 4 * 86400_000, NOW)).toBe('4 天前')
    expect(formatRelative(NOW - 40 * 86400_000, NOW)).toMatch(/^\d{4}-\d{2}-\d{2}$/)
  })
})
describe('b64ToBytes', () => {
  it('解码', () => { expect(new TextDecoder().decode(b64ToBytes('aGk='))).toBe('hi') })
})
describe('basename', () => {
  it('取最后一段', () => { expect(basename('/Users/x/aTerm')).toBe('aTerm') })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现**

```ts
// src/time.ts
export function formatRelative(ms: number, now: number = Date.now()): string {
  const diff = now - ms
  if (diff < 60_000) return '刚刚'
  if (diff < 3600_000) return `${Math.floor(diff / 60_000)} 分钟前`
  if (diff < 24 * 3600_000) return `${Math.floor(diff / 3600_000)} 小时前`
  if (diff < 48 * 3600_000) return '昨天'
  if (diff < 30 * 86400_000) return `${Math.floor(diff / 86400_000)} 天前`
  const d = new Date(ms)
  const p = (n: number) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`
}
export function basename(p: string): string {
  const parts = p.split('/').filter(Boolean)
  return parts[parts.length - 1] ?? p
}
```
```ts
// src/b64.ts
export function b64ToBytes(b64: string): Uint8Array {
  const bin = atob(b64)
  const out = new Uint8Array(bin.length)
  for (let i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i)
  return out
}
```
```ts
// src/ipc.ts
import { invoke } from '@tauri-apps/api/core'

export interface ThreadInfo { rootKey: string; resumeSessionId: string; title: string; cwd: string; lastActivityMs: number; fileCount: number }
export interface ProjectInfo { dirName: string; cwd: string; lastActivityMs: number; threads: ThreadInfo[] }

export const listProjects = () => invoke<ProjectInfo[]>('list_projects')
export const ptySpawn = (o: { cwd?: string; inject?: string; cols: number; rows: number }) => invoke<string>('pty_spawn', o)
export const ptyWrite = (id: string, data: string) => invoke<void>('pty_write', { id, data })
export const ptyResize = (id: string, cols: number, rows: number) => invoke<void>('pty_resize', { id, cols, rows })
export const ptyKill = (id: string) => invoke<void>('pty_kill', { id })
export const ptyIsAlive = (id: string) => invoke<boolean>('pty_is_alive', { id })
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src
git commit -m "feat(ui): ipc 契约、base64、相对时间工具

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 标签页 store 与会话数据 store

**Files:**
- Create: `src/store/tabs.ts`、`src/store/sessions.ts`
- Test: `src/__tests__/tabs.test.ts`

**Interfaces:**
- Consumes: `ipc.ts` 的 `ptySpawn/ptyIsAlive/ptyKill/listProjects`
- Produces:
  - `type Tab = { id: string; kind: 'home' | 'term'; title: string; ptyId?: string }`
  - `useTabs`：`{ tabs: Tab[]; activeId: string; setActive(id): void; openTerminal(o: { title: string; cwd?: string; inject?: string }): Promise<void>; closeTab(id, confirmFn?: (msg: string) => Promise<boolean>): Promise<void> }`——home 标签固定为第一个、不可关闭；closeTab 对仍存活的 PTY 先经 confirmFn 确认再 kill（confirmFn 缺省用 plugin-dialog 的 `confirm`，测试注入假函数）
  - `useSessions`：`{ projects: ProjectInfo[]; loading: boolean; refresh(): Promise<void> }`

- [ ] **Step 1: 写失败测试**

```ts
// src/__tests__/tabs.test.ts
import { beforeEach, describe, expect, it, vi } from 'vitest'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(async () => 'pty-9'),
  ptyIsAlive: vi.fn(async () => true),
  ptyKill: vi.fn(async () => {}),
  listProjects: vi.fn(async () => []),
}))
import * as ipc from '../ipc'
import { useTabs } from '../store/tabs'

beforeEach(() => {
  useTabs.setState({ tabs: [{ id: 'home', kind: 'home', title: '主页' }], activeId: 'home' })
  vi.clearAllMocks()
})

describe('useTabs', () => {
  it('openTerminal 生成 term 标签并激活', async () => {
    await useTabs.getState().openTerminal({ title: '修复登录', cwd: '/tmp/p', inject: 'claude --resume abc' })
    const { tabs, activeId } = useTabs.getState()
    expect(tabs).toHaveLength(2)
    expect(tabs[1]).toMatchObject({ kind: 'term', title: '修复登录', ptyId: 'pty-9' })
    expect(activeId).toBe(tabs[1].id)
    expect(ipc.ptySpawn).toHaveBeenCalledWith({ cwd: '/tmp/p', inject: 'claude --resume abc', cols: 80, rows: 24 })
  })
  it('closeTab：存活 PTY 需确认，拒绝则不关', async () => {
    await useTabs.getState().openTerminal({ title: 't' })
    const id = useTabs.getState().tabs[1].id
    await useTabs.getState().closeTab(id, async () => false)
    expect(useTabs.getState().tabs).toHaveLength(2)
    await useTabs.getState().closeTab(id, async () => true)
    expect(useTabs.getState().tabs).toHaveLength(1)
    expect(ipc.ptyKill).toHaveBeenCalledWith('pty-9')
    expect(useTabs.getState().activeId).toBe('home')
  })
  it('home 不可关闭', async () => {
    await useTabs.getState().closeTab('home', async () => true)
    expect(useTabs.getState().tabs).toHaveLength(1)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run`
Expected: tabs.test FAIL（store 不存在）。

- [ ] **Step 3: 实现**

```ts
// src/store/tabs.ts
import { create } from 'zustand'
import { ptyIsAlive, ptyKill, ptySpawn } from '../ipc'

export type Tab = { id: string; kind: 'home' | 'term'; title: string; ptyId?: string }
let nextTab = 1

type ConfirmFn = (msg: string) => Promise<boolean>
async function dialogConfirm(msg: string): Promise<boolean> {
  const { confirm } = await import('@tauri-apps/plugin-dialog')
  return confirm(msg, { title: 'aTerm' })
}

type TabsState = {
  tabs: Tab[]
  activeId: string
  setActive(id: string): void
  openTerminal(o: { title: string; cwd?: string; inject?: string }): Promise<void>
  closeTab(id: string, confirmFn?: ConfirmFn): Promise<void>
}

export const useTabs = create<TabsState>((set, get) => ({
  tabs: [{ id: 'home', kind: 'home', title: '主页' }],
  activeId: 'home',
  setActive: (id) => set({ activeId: id }),
  openTerminal: async ({ title, cwd, inject }) => {
    const ptyId = await ptySpawn({ cwd, inject, cols: 80, rows: 24 })
    const id = `tab-${nextTab++}`
    set((s) => ({ tabs: [...s.tabs, { id, kind: 'term', title, ptyId }], activeId: id }))
  },
  closeTab: async (id, confirmFn = dialogConfirm) => {
    const tab = get().tabs.find((t) => t.id === id)
    if (!tab || tab.kind === 'home') return
    if (tab.ptyId && (await ptyIsAlive(tab.ptyId))) {
      const ok = await confirmFn('进程仍在运行，关闭标签将终止它。确认关闭？')
      if (!ok) return
      await ptyKill(tab.ptyId)
    }
    set((s) => {
      const tabs = s.tabs.filter((t) => t.id !== id)
      const activeId = s.activeId === id ? tabs[tabs.length - 1].id : s.activeId
      return { tabs, activeId }
    })
  },
}))
```
```ts
// src/store/sessions.ts
import { create } from 'zustand'
import { listProjects, type ProjectInfo } from '../ipc'

type SessionsState = { projects: ProjectInfo[]; loading: boolean; refresh(): Promise<void> }
export const useSessions = create<SessionsState>((set) => ({
  projects: [],
  loading: false,
  refresh: async () => {
    set({ loading: true })
    try { set({ projects: await listProjects() }) } finally { set({ loading: false }) }
  },
}))
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add src
git commit -m "feat(ui): 标签页与会话数据 store（关闭确认、home 固定）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: TerminalView + App 骨架（真终端可用）

**Files:**
- Create: `src/components/TerminalView.tsx`、`src/components/TabBar.tsx`
- Modify: `src/App.tsx`（重写模板内容）、`src/App.css`（重写）、`src/main.tsx`（确认只渲染 App）

**Interfaces:**
- Consumes: `useTabs`、`ipc`、`b64ToBytes`、事件 `pty-output`/`pty-exit`
- Produces:
  - `TerminalView({ ptyId, active }: { ptyId: string; active: boolean })`——xterm 实例常驻（隐藏不卸载），active 时 fit+focus
  - `TabBar()`——渲染 tabs、点击切换、×关闭（home 无×）
  - App 布局：左侧 `<Sidebar/>` 占位 div（Task 9 实现）+ 右侧（TabBar + 内容区）；home 内容区暂放按钮「＋ 新终端」（`openTerminal({ title: 'zsh' })`，Task 9 被 HomePage 替换）

- [ ] **Step 1: 实现 TerminalView**

```tsx
// src/components/TerminalView.tsx
import { useEffect, useRef } from 'react'
import { Terminal } from '@xterm/xterm'
import { FitAddon } from '@xterm/addon-fit'
import '@xterm/xterm/css/xterm.css'
import { listen, type UnlistenFn } from '@tauri-apps/api/event'
import { ptyResize, ptyWrite } from '../ipc'
import { b64ToBytes } from '../b64'

export function TerminalView({ ptyId, active }: { ptyId: string; active: boolean }) {
  const elRef = useRef<HTMLDivElement>(null)
  const termRef = useRef<Terminal | null>(null)
  const fitRef = useRef<FitAddon | null>(null)

  useEffect(() => {
    const el = elRef.current!
    const term = new Terminal({
      fontFamily: '"SF Mono", Menlo, monospace',
      fontSize: 13,
      cursorBlink: true,
      theme: { background: '#15161e', foreground: '#c0caf5' },
    })
    const fit = new FitAddon()
    term.loadAddon(fit)
    term.open(el)
    fit.fit()
    termRef.current = term
    fitRef.current = fit
    term.onData((d) => { void ptyWrite(ptyId, d) })

    const unlisteners: UnlistenFn[] = []
    void listen<{ id: string; data: string }>('pty-output', (e) => {
      if (e.payload.id === ptyId) term.write(b64ToBytes(e.payload.data))
    }).then((u) => unlisteners.push(u))
    void listen<{ id: string; code: number }>('pty-exit', (e) => {
      if (e.payload.id === ptyId) term.write('\r\n\x1b[90m[进程已退出，可关闭此标签]\x1b[0m\r\n')
    }).then((u) => unlisteners.push(u))

    const ro = new ResizeObserver(() => {
      if (el.clientWidth === 0) return // 隐藏时跳过
      fit.fit()
      void ptyResize(ptyId, term.cols, term.rows)
    })
    ro.observe(el)
    return () => { ro.disconnect(); unlisteners.forEach((u) => u()); term.dispose() }
  }, [ptyId])

  useEffect(() => {
    if (active) {
      requestAnimationFrame(() => {
        fitRef.current?.fit()
        termRef.current?.focus()
        const t = termRef.current
        if (t) void ptyResize(ptyId, t.cols, t.rows)
      })
    }
  }, [active, ptyId])

  return <div ref={elRef} className="terminal-host" />
}
```

- [ ] **Step 2: 实现 TabBar 与 App**

```tsx
// src/components/TabBar.tsx
import { useTabs } from '../store/tabs'

export function TabBar() {
  const { tabs, activeId, setActive, closeTab } = useTabs()
  return (
    <div className="tabbar">
      {tabs.map((t) => (
        <div key={t.id} className={`tab ${t.id === activeId ? 'active' : ''}`} onClick={() => setActive(t.id)}>
          <span className="tab-title">{t.kind === 'home' ? '⌂' : t.title}</span>
          {t.kind !== 'home' && (
            <span className="tab-close" onClick={(e) => { e.stopPropagation(); void closeTab(t.id) }}>×</span>
          )}
        </div>
      ))}
    </div>
  )
}
```
```tsx
// src/App.tsx
import './App.css'
import { TabBar } from './components/TabBar'
import { TerminalView } from './components/TerminalView'
import { useTabs } from './store/tabs'

export default function App() {
  const { tabs, activeId, openTerminal } = useTabs()
  return (
    <div className="app">
      <aside className="sidebar">{/* Task 9: <Sidebar/> */}</aside>
      <div className="main">
        <TabBar />
        <div className="content">
          <div style={{ display: activeId === 'home' ? 'block' : 'none', padding: 24 }}>
            {/* Task 9 将替换为 <HomePage/> */}
            <button onClick={() => void openTerminal({ title: 'zsh' })}>＋ 新终端</button>
          </div>
          {tabs.filter((t) => t.kind === 'term').map((t) => (
            <div key={t.id} className="term-wrap" style={{ display: activeId === t.id ? 'block' : 'none' }}>
              <TerminalView ptyId={t.ptyId!} active={activeId === t.id} />
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
```
```css
/* src/App.css —— 整文件替换 */
* { box-sizing: border-box; margin: 0; padding: 0; }
html, body, #root { height: 100%; }
body { font-family: -apple-system, 'PingFang SC', sans-serif; background: #1a1b26; color: #c0caf5; }
.app { display: flex; height: 100vh; }
.sidebar { width: 230px; flex: none; background: #16161e; border-right: 1px solid #2a2b3a; overflow-y: auto; padding: 10px; }
.main { flex: 1; display: flex; flex-direction: column; min-width: 0; }
.tabbar { display: flex; gap: 4px; padding: 6px 8px 0; background: #16161e; border-bottom: 1px solid #2a2b3a; overflow-x: auto; }
.tab { display: flex; align-items: center; gap: 6px; padding: 5px 12px; border-radius: 7px 7px 0 0; background: #1f2030; color: #787c99; cursor: default; white-space: nowrap; font-size: 13px; }
.tab.active { background: #1a1b26; color: #c0caf5; }
.tab-close { padding: 0 2px; border-radius: 4px; }
.tab-close:hover { background: #3b3d57; color: #fff; }
.content { flex: 1; min-height: 0; position: relative; overflow-y: auto; }
.term-wrap { position: absolute; inset: 0; background: #15161e; }
.terminal-host { width: 100%; height: 100%; padding: 4px 0 0 6px; }
.section-label { font-size: 11px; color: #565a7a; text-transform: uppercase; letter-spacing: .08em; margin: 14px 0 8px; }
.side-item { padding: 6px 8px; border-radius: 6px; margin-bottom: 2px; font-size: 13px; cursor: default; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.side-item:hover { background: #1f2030; }
.side-item .sub { color: #565a7a; font-size: 11px; }
.home { padding: 28px 32px; max-width: 980px; margin: 0 auto; }
.cmd-input { width: 100%; padding: 12px 16px; border-radius: 10px; border: 1px solid #2a2b3a; background: #16161e; color: #c0caf5; font-size: 14px; font-family: 'SF Mono', Menlo, monospace; outline: none; }
.cmd-input:focus { border-color: #3d59a1; }
.cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 12px; margin-top: 12px; }
.card { border: 1px solid #2a2b3a; border-radius: 10px; background: #16161e; padding: 12px; cursor: default; }
.card:hover { border-color: #3d59a1; }
.card .name { font-weight: 600; font-size: 14px; }
.card .sub { color: #565a7a; font-size: 12px; margin-top: 4px; }
.thread-list { margin-top: 8px; border-top: 1px solid #2a2b3a; padding-top: 6px; }
.thread-row { display: flex; justify-content: space-between; gap: 8px; padding: 5px 6px; border-radius: 6px; font-size: 12.5px; }
.thread-row:hover { background: #1f2030; }
.thread-row .t { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.thread-row .time { color: #565a7a; flex: none; }
.new-conv { color: #7aa2f7; }
```
`src/main.tsx` 保持模板（StrictMode 渲染 `<App/>`）。删除模板遗留的 `greet` 调用与无用资源（logo 等）。
**StrictMode 注意**：开发模式下 effect 双跑会创建两个 xterm 实例，清理函数已 dispose 旧实例，行为正确；若出现双回显，检查清理函数是否完整执行。

- [ ] **Step 3: 真机验证（手动清单）**

Run: `npm run tauri dev`，依次验证——
1. 点「＋ 新终端」→ 出现 zsh 提示符（登录 shell，PATH 含 `~/.local/bin`：跑 `which claude` 有输出）
2. `ls`、`vim`（进入/编辑/`:q`）、`htop` 或 `top`（`q` 退出）正常渲染
3. 中文输入法：输入中文到命令行，无乱码（`echo 你好`）
4. 拖动窗口大小 → 终端自动重排（`tput cols` 数值变化）
5. 标签：开 3 个终端来回切换，内容不丢；×关闭运行中标签弹出确认对话框
6. 在终端里 `exit` → 显示「[进程已退出，可关闭此标签]」，再关闭无确认弹窗

Expected: 全部通过。任何一条失败：先修再继续。

- [ ] **Step 4: Commit**

```bash
git add src
git commit -m "feat(ui): xterm 终端视图与标签骨架（常驻实例、resize、关闭确认）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 9: HomePage + Sidebar + 恢复/新建动作

**Files:**
- Create: `src/actions.ts`、`src/components/HomePage.tsx`、`src/components/Sidebar.tsx`
- Modify: `src/App.tsx`（挂上两组件，替换占位）
- Test: `src/__tests__/HomePage.test.tsx`

**Interfaces:**
- Consumes: `useTabs.openTerminal`、`useSessions`、`ipc` 类型、`formatRelative/basename`
- Produces（`src/actions.ts`）:
  - `resumeThread(cwd: string, t: ThreadInfo): Promise<void>` → `openTerminal({ title: t.title, cwd, inject: \`claude --resume ${t.resumeSessionId}\` })`
  - `newConversation(cwd: string): Promise<void>` → `openTerminal({ title: '新对话', cwd, inject: 'claude' })`
  - `runCommand(cmd: string): Promise<void>` → 空串开纯 zsh；否则 `openTerminal({ title: cmd.slice(0, 24), inject: cmd })`

- [ ] **Step 1: 写失败测试**

```tsx
// src/__tests__/HomePage.test.tsx
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('../ipc', () => ({
  ptySpawn: vi.fn(async () => 'pty-1'),
  ptyIsAlive: vi.fn(async () => false),
  ptyKill: vi.fn(async () => {}),
  listProjects: vi.fn(async () => []),
}))
import * as ipc from '../ipc'
import { useSessions } from '../store/sessions'
import { useTabs } from '../store/tabs'
import { HomePage } from '../components/HomePage'

const PROJECTS = [{
  dirName: '-Users-x-phineuro', cwd: '/Users/x/phineuro', lastActivityMs: Date.now() - 60_000,
  threads: [
    { rootKey: 'u1', resumeSessionId: 'sid-1', title: '修复登录流程', cwd: '/Users/x/phineuro', lastActivityMs: Date.now() - 60_000, fileCount: 2 },
    { rootKey: 'u2', resumeSessionId: 'sid-2', title: '写测试', cwd: '/Users/x/phineuro', lastActivityMs: Date.now() - 3600_000, fileCount: 1 },
  ],
}]

beforeEach(() => {
  useSessions.setState({ projects: PROJECTS as never, loading: false })
  useTabs.setState({ tabs: [{ id: 'home', kind: 'home', title: '主页' }], activeId: 'home' })
  vi.clearAllMocks()
})

describe('HomePage', () => {
  it('渲染项目卡片，点击展开会话，点会话触发 resume', async () => {
    render(<HomePage />)
    const card = screen.getByText('phineuro')
    fireEvent.click(card)
    const row = await screen.findByText('修复登录流程')
    fireEvent.click(row)
    await vi.waitFor(() => {
      expect(ipc.ptySpawn).toHaveBeenCalledWith(
        expect.objectContaining({ cwd: '/Users/x/phineuro', inject: 'claude --resume sid-1' }),
      )
    })
  })
  it('命令输入框回车开新标签执行命令', async () => {
    render(<HomePage />)
    const input = screen.getByPlaceholderText(/输入命令/)
    fireEvent.change(input, { target: { value: 'htop' } })
    fireEvent.keyDown(input, { key: 'Enter' })
    await vi.waitFor(() => {
      expect(ipc.ptySpawn).toHaveBeenCalledWith(expect.objectContaining({ inject: 'htop' }))
    })
  })
  it('展开卡片里的「＋ 新对话」注入 claude', async () => {
    render(<HomePage />)
    fireEvent.click(screen.getByText('phineuro'))
    fireEvent.click(await screen.findByText('＋ 新对话'))
    await vi.waitFor(() => {
      expect(ipc.ptySpawn).toHaveBeenCalledWith(expect.objectContaining({ cwd: '/Users/x/phineuro', inject: 'claude' }))
    })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run`
Expected: HomePage.test FAIL（组件不存在）。

- [ ] **Step 3: 实现**

```ts
// src/actions.ts
import type { ThreadInfo } from './ipc'
import { useTabs } from './store/tabs'

export const resumeThread = (cwd: string, t: ThreadInfo) =>
  useTabs.getState().openTerminal({ title: t.title, cwd, inject: `claude --resume ${t.resumeSessionId}` })

export const newConversation = (cwd: string) =>
  useTabs.getState().openTerminal({ title: '新对话', cwd, inject: 'claude' })

export const runCommand = (cmd: string) => {
  const c = cmd.trim()
  return useTabs.getState().openTerminal(c ? { title: c.slice(0, 24), inject: c } : { title: 'zsh' })
}
```
```tsx
// src/components/HomePage.tsx
import { useState } from 'react'
import { newConversation, resumeThread, runCommand } from '../actions'
import { useSessions } from '../store/sessions'
import { basename, formatRelative } from '../time'

export function HomePage() {
  const { projects } = useSessions()
  const [cmd, setCmd] = useState('')
  const [expanded, setExpanded] = useState<string | null>(null)
  return (
    <div className="home">
      <input
        className="cmd-input"
        placeholder="输入命令，回车在新标签页运行…（留空回车 = 新终端）"
        value={cmd}
        onChange={(e) => setCmd(e.target.value)}
        onKeyDown={(e) => { if (e.key === 'Enter') { void runCommand(cmd); setCmd('') } }}
      />
      <div className="section-label">最近项目</div>
      <div className="cards">
        {projects.map((p) => (
          <div key={p.dirName} className="card" onClick={() => setExpanded(expanded === p.dirName ? null : p.dirName)}>
            <div className="name">📁 {basename(p.cwd)}</div>
            <div className="sub">{p.threads.length} 个会话 · {formatRelative(p.lastActivityMs)}</div>
            {expanded === p.dirName && (
              <div className="thread-list" onClick={(e) => e.stopPropagation()}>
                {p.threads.slice(0, 8).map((t) => (
                  <div key={t.rootKey} className="thread-row" onClick={() => void resumeThread(p.cwd, t)}>
                    <span className="t">{t.title}</span>
                    <span className="time">{formatRelative(t.lastActivityMs)}</span>
                  </div>
                ))}
                <div className="thread-row new-conv" onClick={() => void newConversation(p.cwd)}>＋ 新对话</div>
              </div>
            )}
          </div>
        ))}
        {projects.length === 0 && <div className="sub">尚未发现 Claude Code 会话（~/.claude/projects 为空）</div>}
      </div>
    </div>
  )
}
```
```tsx
// src/components/Sidebar.tsx
import { resumeThread } from '../actions'
import { useSessions } from '../store/sessions'
import { basename, formatRelative } from '../time'

export function Sidebar() {
  const { projects } = useSessions()
  const recent = projects
    .flatMap((p) => p.threads.map((t) => ({ p, t })))
    .sort((a, b) => b.t.lastActivityMs - a.t.lastActivityMs)
    .slice(0, 12)
  return (
    <>
      <div className="section-label">最近会话</div>
      {recent.map(({ p, t }) => (
        <div key={t.rootKey} className="side-item" title={t.title} onClick={() => void resumeThread(p.cwd, t)}>
          {t.title}
          <div className="sub">{basename(p.cwd)} · {formatRelative(t.lastActivityMs)}</div>
        </div>
      ))}
    </>
  )
}
```
`App.tsx`：`<aside className="sidebar"><Sidebar /></aside>`；home 占位替换为 `<HomePage />`（display 切换逻辑保持）；顶部加：
```tsx
import { useEffect } from 'react'
import { HomePage } from './components/HomePage'
import { Sidebar } from './components/Sidebar'
import { useSessions } from './store/sessions'
// App 组件体内：
const refresh = useSessions((s) => s.refresh)
useEffect(() => {
  void refresh()
  const onFocus = () => void refresh()
  window.addEventListener('focus', onFocus)
  return () => window.removeEventListener('focus', onFocus)
}, [refresh])
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run`
Expected: 全部 PASS。

- [ ] **Step 5: 真机验证**

Run: `npm run tauri dev`
1. 主页显示你的真实项目卡片（phineuro 等），按时间降序
2. 点卡片展开会话列表 → 点一条 → 新标签打开并自动执行 `claude --resume <id>`，对话恢复
3. 侧边栏「最近会话」点击同样恢复
4. 「＋ 新对话」→ 新标签自动跑 `claude`；退出后切回主页点窗口（触发 focus 刷新）→ 新会话出现在列表
5. 命令输入框输 `echo hi` 回车 → 新标签执行

Expected: 全部通过。

- [ ] **Step 6: Commit**

```bash
git add src
git commit -m "feat(ui): 启动器式主页、侧边栏、恢复/新建对话与焦点刷新

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 10: P1 收尾——验收清单 + README

**Files:**
- Create: `README.md`
- Modify: 验收中发现的任何问题就地修复

- [ ] **Step 1: 全量测试**

Run: `cd src-tauri && cargo test && cd .. && npx vitest run && npm run tauri build`
Expected: 测试全过；build 产出 `src-tauri/target/release/bundle/macos/aTerm.app`。

- [ ] **Step 2: 装机验收（用 build 出的 aTerm.app 跑，不是 dev）**

打开 `aTerm.app`，完整过一遍：
1. 主页项目卡片与真实 `~/.claude/projects` 一致；同一对话不重复（会话链）
2. 恢复一条昨天的真实对话 → Claude Code 完整恢复历史
3. 新对话 → 用完退出 → focus 刷新后出现在主页
4. 终端日用：zsh 插件、vim、中文 IME、resize、多标签
5. 关闭运行中标签有确认；已退出标签直接关
6. `~/.claude/` 目录零写入：验收前 `ls -laR ~/.claude/projects | shasum` 记录，验收后比对一致
Expected: 全部通过；有问题修复后重跑该项。

- [ ] **Step 3: 写 README**

```markdown
# aTerm

macOS 上专为 Claude Code agent 工作流设计的终端（Tauri 2 + React + xterm.js）。

设计文档：docs/superpowers/specs/2026-08-24-aterm-design.md
当前进度：P1（终端 + 主页 + 会话恢复）✅；P2 总览方块页开发中。

## 开发
npm install && npm run tauri dev
测试：cd src-tauri && cargo test；npx vitest run
打包：npm run tauri build
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: README 与 P1 验收完成

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review 记录

- **Spec 覆盖**：P1 行的六项——Tauri 壳(T1)、xterm 终端标签(T5/T8)、启动器主页(T9)、侧边栏(T9)、点击恢复+会话链(T2/T3/T9)、新建对话(T9)；静态信息约束在 T9（无状态色）；`~/.claude` 只读在 T10 验收硬校验。spec §6.1「索引缓存」P1 以头尾读取达成秒开（约 50 文件 <100ms），持久缓存推迟到 P2 与监听器同做——记录为有意决策。
- **占位符扫描**：无 TBD/TODO；所有代码块完整。
- **类型一致性**：`ThreadInfo/ProjectInfo` 字段 Rust(camelCase serde) 与 TS 逐字段核对一致；命令名 `list_projects/pty_*` 三处（Rust 定义、注册、ipc.ts）一致；事件名 `pty-output/pty-exit` 与载荷 `{id,data}/{id,code}` 两端一致。
